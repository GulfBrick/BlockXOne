apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus-blockxone
    url: http://prometheus:9090
    access: proxy
    isDefault: true
    jsonData:
      timeInterval: 15s

  - name: Loki
    type: loki
    uid: loki-blockxone
    url: http://loki:3100
    access: proxy
    isDefault: false
    jsonData: {}
