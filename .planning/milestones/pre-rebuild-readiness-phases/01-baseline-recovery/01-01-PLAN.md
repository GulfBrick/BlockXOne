---
phase: 01-baseline-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - apps/web/.eslintrc.json
  - apps/web/package.json
  - apps/web/package-lock.json
  - apps/web/src/app/investor/funds/[id]/page.tsx
  - apps/web/src/app/login/page.tsx
  - apps/web/src/components/examples/DesignSystemShowcase.tsx
  - apps/web/src/components/ui/sidebar-nav.tsx
  - apps/web/src/components/wallet/WalletWidget.tsx
  - apps/web/src/lib/auth-context-v2.tsx
  - cmd/api/main.go
  - cmd/api/routes.go
  - internal/auth/auth.go
  - internal/auth/auth_test.go
  - internal/ethsig/ethsig_test.go
  - internal/examples/providers_usage.go
  - internal/policy/policy.go
  - internal/policy/policy_test.go
  - internal/rbac/rbac.go
autonomous: true
user_setup:
  - Copy `.env.example` to `.env` if the local env file is missing.
  - Run `docker compose up -d` before migrations/seed and API startup.
must_haves:
  truths:
    - All Go command binaries in scope build successfully from this checkout.
    - Backend unit and package tests in `./cmd/...` and `./internal/...` pass with the required `GOPROXY` override.
    - The Next.js app completes a production build on port `3000` without blocking type errors.
    - Local dependency services and the API/web localhost contract are verifiably alive.
  artifacts:
    - path: .planning/phases/01-baseline-recovery/01-VERIFICATION.md
      provides: Recorded verification evidence for Phase 1 completion.
    - path: README.md
      provides: Documented local startup contract for the recovered baseline.
    - path: apps/web/package.json
      provides: Locked local frontend port and added missing table dependency.
  key_links:
    - from: internal/auth/auth.go
      to: internal/rbac/rbac.go
      via: Interface-based DB contract for middleware and RBAC loading.
    - from: internal/policy/policy.go
      to: internal/policy/policy_test.go
      via: QueryRow interface boundary that keeps policy logic testable.
    - from: apps/web/src/components/wallet/WalletWidget.tsx
      to: apps/web/src/lib/auth-context-v2.tsx
      via: Typed frontend auth and wallet flow that now passes production build checks.
---

# Phase 1 Plan 01: Recover A Trustworthy Local Baseline

## Objective

- **What:** Repair the repo until backend builds/tests, frontend production build, and local runtime entrypoints all succeed from the current checkout.
- **Why:** Every downstream production-readiness phase depends on a baseline that is actually executable and testable.
- **Output:** Verified Phase 1 artifacts, green backend test/build checks, green frontend production build, and confirmed localhost health for web and API.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STACK.md

## Tasks

<task type="code">
  <name>Stabilize backend compile and test surfaces</name>
  <files>internal/auth/auth.go, internal/auth/auth_test.go, internal/policy/policy.go, internal/policy/policy_test.go, internal/rbac/rbac.go, internal/ethsig/ethsig_test.go, internal/examples/providers_usage.go</files>
  <action>Replace concrete pool coupling with interfaces where appropriate, repair broken test fixtures, and remove example/test drift that blocked `go test`.</action>
  <verify>`go test ./cmd/... ./internal/...` passes with `GOPROXY=https://proxy.golang.org,direct`.</verify>
  <done>All package tests in scope pass without editing around the failures or disabling the test set.</done>
</task>

<task type="code">
  <name>Unblock the frontend production build</name>
  <files>apps/web/.eslintrc.json, apps/web/package.json, apps/web/package-lock.json, apps/web/src/app/investor/funds/[id]/page.tsx, apps/web/src/app/login/page.tsx, apps/web/src/components/examples/DesignSystemShowcase.tsx, apps/web/src/components/ui/sidebar-nav.tsx, apps/web/src/components/wallet/WalletWidget.tsx, apps/web/src/lib/auth-context-v2.tsx</files>
  <action>Fix the real hook/type/runtime problems, add the missing table dependency, and downgrade non-blocking brownfield lint debt to warnings so the production build can finish honestly.</action>
  <verify>`npm run build` succeeds in `apps/web`.</verify>
  <done>The Next.js production build completes without hard type or runtime-compile failures.</done>
</task>

<task type="ops">
  <name>Re-verify localhost and dependency contract</name>
  <files>README.md, docker-compose.yml, cmd/api/main.go, apps/web/package.json</files>
  <action>Confirm Docker dependencies are up, verify API health on `8080`, and verify web routes on `3000` after restarting the dev server when necessary.</action>
  <verify>`docker compose ps`, `Invoke-WebRequest http://localhost:8080/healthz`, and `Invoke-WebRequest http://localhost:3000/login` succeed.</verify>
  <done>The local contract is proven rather than assumed, and the docs/runtime values no longer disagree.</done>
</task>

## Verification

Run:
- `docker compose up -d`
- `$env:GOPROXY='https://proxy.golang.org,direct'; go test ./cmd/... ./internal/...`
- `$env:GOPROXY='https://proxy.golang.org,direct'; go build -mod=mod ./cmd/api; go build -mod=mod ./cmd/worker; go build -mod=mod ./cmd/migrate; go build -mod=mod ./cmd/seed`
- `cd apps/web && npm run build`
- `Invoke-WebRequest http://localhost:8080/healthz`
- `Invoke-WebRequest http://localhost:3000/login`

## Success Criteria

- [x] Backend packages in scope build and test successfully.
- [x] Frontend production build completes.
- [x] Docker-backed local dependencies report healthy running status.
- [x] Web and API localhost entrypoints respond on the standardized ports.
