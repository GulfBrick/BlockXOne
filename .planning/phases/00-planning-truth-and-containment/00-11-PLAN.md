---
phase: 00-planning-truth-and-containment
plan: 11
type: execute
name: generation 3 ignored-output evidence truth correction
status: proposed
gap_closure: true
wave: 1
depends_on: [00-10]
autonomous: true
risk_class: critical
requirements: [BASE-03, BASE-04, BASE-06, BASE-07]
user_setup: []
execution_mode: controller-dispatched-serial-tasks
requirements_completion: forbidden
files_modified:
  - .planning/phases/00-planning-truth-and-containment/00-11-PLAN.md
  - .planning/TEST-CONTRACT.md
  - .planning/scripts/validate-planning.ps1
  - .planning/BLOCKERS.md
  - .planning/EVIDENCE-REGISTER.md
  - .planning/STATE.md
  - .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md
  - .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md
  - .planning/phases/00-planning-truth-and-containment/00-SECURITY-REVIEW.md
  - .planning/phases/00-planning-truth-and-containment/00-VERIFICATION.md
must_haves:
  truths:
    - "V3 remains preserved but unadmitted because Task 9 found ignored generated residue and stale generated-free evidence."
    - "The corrected planning and release gates inspect ignored as well as ordinary tracked and untracked worktree state."
    - "Plan 00-11 raises the GSD artifact count to eleven while authoritative production completion remains zero."
    - "I4, R4, V4 and C4 preserve distinct builder, reviewer, verifier and final-admission roles."
    - "No product behavior, product source, ROADMAP, requirement checkbox or professional or production gate changes."
  artifacts:
    - ".planning/phases/00-planning-truth-and-containment/00-11-PLAN.md"
    - ".planning/TEST-CONTRACT.md"
    - ".planning/scripts/validate-planning.ps1"
    - ".planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md"
  key_links:
    - "V3 failed admission -> P10 accepted corrective plan -> I4 corrected evidence freeze."
    - "I4 -> R4 -> V4 preserves one-file independent review and verification commits."
    - "Exact V4 -> external CANDIDATE-MANIFEST-v4.md -> local-only C4=V4."
---

# Plan 00-11: Generation 3 ignored-output evidence truth correction

## Objective

<objective>
Correct the late Task 9 evidence-truth failure without amending or silently reclassifying any earlier commit or receipt.
Make ignored generated residue a
first-class fail-closed release condition, rebuild the evidence lineage through
distinct roles, and admit a new local-only candidate only after exact-V4
reproduction.
</objective>

## Authority boundary

The user's 2026-07-23 approval covers continued local planning, recoverable
cleanup of the exact ignored generated file, local tests, local evidence,
independent review, independent verification and local commits. It does not
authorize a remote, push, deployment, RPC, provider, wallet, key, funds, real
assets, production network, production configuration, professional approval or
irreversible action.

This plan is a controlled correction inside Phase 0. It must not run phase
completion, requirement completion, ROADMAP progress completion or
auto-advance. Phase 0 remains `in_progress`; release remains `NO-GO`.

## Trigger and typed root cause

Task 9 froze exact V3
`37abb9517f86c79b541209bed66f622fae153b7f` and correctly stopped before
running the matrix or creating a candidate manifest because:

- `git status --porcelain=v1 --untracked-files=all --ignored=matching`
  returned `!! apps/web/next-env.d.ts`;
- the file is untracked and ignored by `apps/web/.gitignore:36`;
- it is 268 bytes with SHA-256
  `F4E8976C19FC926644D72610BF1058BD6BF52ADD97E46A02BC0B912A751625C0`;
- its recorded creation and last-write time is 2026-07-23 18:06:07 local;
- the builder, reviewer and verifier checked closed enumerations of generated
  directories that omitted this generated file; and
- internal and external evidence used broader generated-path-free or
  generated-free language.

The typed root cause is `EVIDENCE_TRUTH_IGNORED_GENERATED_PATH_OMISSION`.
V3 is preserved evidence of the failed admission attempt. It is not C3 and
must never be described as admitted.

## Corrective lineage and immutable boundary

| Boundary | Exact contract |
| --- | --- |
| Failed candidate | `V3 = 37abb9517f86c79b541209bed66f622fae153b7f`; preserved and unadmitted |
| Corrective plan | `P10`; parent V3; changes only this Plan 00-11 |
| Corrected evidence freeze | `I4`; parent P10; changes exactly the seven Task 2 paths |
| Independent review | `R4`; parent I4; changes only `00-SECURITY-REVIEW.md` |
| Independent verification | `V4`; parent R4; changes only `00-VERIFICATION.md` |
| Local candidate | `C4 = V4`; external manifest only; no Git change |
| Immutable baseline | C2 and its v2 manifest remain byte-for-byte unchanged |
| Retry ceiling | This is remediation cycle one for the typed root cause |

Never amend or overwrite I3, R3, V3 or their external receipts. Preserve the
old statements as historical evidence, then explicitly supersede their
generated-cleanliness conclusion in the new files and receipts.

Repository changes from V3 through V4 are limited to the frontmatter
`files_modified` allowlist. Product code, application tests, dependency locks,
CI, Compose, contracts, ROADMAP.md, REQUIREMENTS.md, the original plans and
proof scripts outside the listed validator are immutable.

The external evidence directory remains:

`C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721`

New deterministic external filenames are:

- `GEN3-V3-TASK9-FAILURE-RECEIPT.md`;
- `GEN3-P10-EVIDENCE-TRUTH-PLAN-ACCEPTANCE.md`;
- `TASK6-I4-BUILDER-RECEIPT.md`;
- `SECURITY-REVIEW-RECEIPT-v4.md`;
- `VERIFICATION-RECEIPT-v4.md`; and
- `CANDIDATE-MANIFEST-v4.md`.

<tasks>

<task type="auto" category="plan" id="00-11-01">
  <name>Task 1: Freeze the failed V3 admission and accept P10</name>
  <files>.planning/phases/00-planning-truth-and-containment/00-11-PLAN.md; external GEN3-V3-TASK9-FAILURE-RECEIPT.md and GEN3-P10-EVIDENCE-TRUTH-PLAN-ACCEPTANCE.md</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-04-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-10-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-11-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md,
    .planning/phases/00-planning-truth-and-containment/00-SECURITY-REVIEW.md,
    .planning/phases/00-planning-truth-and-containment/00-VERIFICATION.md,
    all Generation 3 external receipts
  </read_first>
  <action>
    Keep the Task 9 agent held. At exact V3, record HEAD, tree, parent, branch,
    zero remotes, ordinary status and ignored-aware status. Record the exact
    next-env.d.ts path, size, SHA-256, timestamps, `git ls-files` nonmembership
    and `git check-ignore -v` provenance in
    `GEN3-V3-TASK9-FAILURE-RECEIPT.md`. State that no matrix, scanner or
    manifest ran and V3 was not admitted.

    An independent plan checker who did not author this file must review Plan
    00-11 against Plans 00-04 through 00-10, all current evidence and the exact
    worktree fact. Resolve every critical issue before acceptance.

    Create a fresh OS-temporary directory named with a GUID. Resolve the source
    as the exact file `apps/web/next-env.d.ts` beneath the resolved repository
    root and the destination beneath the resolved temporary root. Require the
    destination to be absent. Move this one file without overwrite and without
    recursive deletion. Record source/destination, 268-byte size, SHA-256,
    timestamps and ignore provenance. Re-run the exact ignored-aware status
    command and require its only output to be
    `?? .planning/phases/00-planning-truth-and-containment/00-11-PLAN.md`.

    Read-only snapshot the immutable C2 repository before P10 acceptance.
    Preserve its ordinary Git status, ignored-aware status and a relative-path,
    size and SHA-256 inventory of every ignored file. In particular, disclose
    the pre-existing ignored `apps/web/.npm-cache/` residue rather than calling
    C2 filesystem-clean. Do not build in, clean, move from or otherwise mutate
    C2. Bind this ignored-file inventory in the P10 acceptance receipt for the
    final C4 equality check.

    Commit only Plan 00-11 as P10 with exact V3 as parent. Write
    `GEN3-P10-EVIDENCE-TRUTH-PLAN-ACCEPTANCE.md` with V3/P10 commit and tree,
    plan SHA-256, independent checker identity and verdict, failure-receipt
    hash, quarantine evidence, one-branch/zero-remote proof, ordinary and
    ignored-aware clean status, silent strict/unreachable fsck and
    physical-equals-reachable counts.
  </action>
  <verify>
    `parent(P10)=V3`; `git diff --name-only V3..P10` is exactly
    `00-11-PLAN.md`; both external files exist and hash; ignored-aware status is
    empty; zero remotes; strict and full-unreachable fsck are silent.
  </verify>
  <acceptance_criteria>
    - The failure receipt contains the exact V3 identity and exact
      `!! apps/web/next-env.d.ts` failure.
    - The file is preserved at a unique recoverable destination with matching
      268-byte size and SHA-256.
    - P10 is plan-only, independently accepted and clean under both ordinary
      and ignored-aware status.
    - C2 is unchanged and its known ignored-file inventory is explicitly
      recorded without a filesystem-clean claim.
    - V3 and every old receipt remain byte-for-byte unchanged and unadmitted.
  </acceptance_criteria>
  <done>P10 truthfully freezes the failed V3 gate and the accepted correction.</done>
  <commit>docs(phase-00): plan ignored-output evidence correction</commit>
</task>

<task type="auto" category="evidence" id="00-11-02">
  <name>Task 2: Make ignored residue fail closed and freeze I4</name>
  <files>
    .planning/TEST-CONTRACT.md,
    .planning/scripts/validate-planning.ps1,
    .planning/BLOCKERS.md,
    .planning/EVIDENCE-REGISTER.md,
    .planning/STATE.md,
    .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md,
    .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md
  </files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-11-PLAN.md,
    .planning/TEST-CONTRACT.md,
    .planning/scripts/validate-planning.ps1,
    .planning/BLOCKERS.md,
    .planning/EVIDENCE-REGISTER.md,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md,
    .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md
  </read_first>
  <action>
    Use a builder who did not author or check Plan 00-11 and who will not act as
    R4 reviewer, V4 verifier or C4 admission owner.

    Replace the current TEST-CONTRACT generated cleanup that uses
    `Remove-Item -Recurse -Force` with recoverable no-overwrite quarantine.
    It must create a unique absent directory beneath the resolved OS temporary
    root, resolve every source beneath its exact repository component root,
    require every destination to be absent, use literal-path moves, record the
    source/destination and verify the source is absent after the move. It must
    never recursively delete generated output. Include web and contract
    dependencies, build output, caches, coverage and TypeChain paths, with
    distinct destinations for nested paths.

    Make `apps/web/next-env.d.ts` an explicit generated web candidate and make
    the post-cleanup release boundary execute
    `git status --porcelain=v1 --untracked-files=all --ignored=matching`.
    Any output is failure. The contract must distinguish recoverable evidence
    quarantine from a clean candidate assertion and must not represent normal
    `git status` as sufficient.

    Update `validate-planning.ps1` to recognize exactly Plans 00-01 through
    00-11, exactly one `00-04-SUMMARY.md`, GSD artifact status `partial`,
    artifact progress 9, STATE total_plans 11/completed_plans 0, Phase 0
    in_progress, ROADMAP and all 89 requirements incomplete. Add a read-only
    repository residue assertion using the exact ignored-aware status command.
    Any ordinary untracked (`??`) or ignored (`!!`) entry, including
    `apps/web/next-env.d.ts`, must fail. Tracked edits remain governed by the
    existing exact builder-worktree allowlist during staged proofs; the
    TEST-CONTRACT clean-candidate mode must reject every tracked edit as well.
    The validator must never delete or move a residue.

    Update the other five builder-evidence files to record Plan 00-11, P10, the
    Task 9 failure, the preserved V3 disposition, the exact quarantine, the
    corrected 11-plan/partial-9 semantics and all retained NO-GO blockers.
    ROADMAP.md and REQUIREMENTS.md remain byte-identical. STATE authoritative
    production completion remains zero.

    In isolated disposable copies, prove two validator states: Plan 00-11 with
    no recognized summary is `planned/0`; the real summary-bearing builder
    state is `partial/9`. Mutation-test exact plan names/count, summary
    name/metadata, progress 9, STATE total 11, ROADMAP/requirements
    incompleteness, and ignored residue. At minimum, creating the exact ignored
    `apps/web/next-env.d.ts` fixture must make validation fail; removing only
    that fixture must restore a pass. Every mutation restores byte-identical
    originals.

    Commit exactly the seven listed paths as I4 with P10 as parent. At exact I4
    run the complete TEST-CONTRACT with pinned Node 22.23.1/npm 10.9.8 and Go
    1.26.5, every planning/repository/CI/Compose/actionlint gate, Go
    test/vet/lint/gosec/govulncheck gate, web preflight/config/runner/unit/lint/
    build/four-audit/containment gate, contract preflight/compile/typecheck/
    full-and-targeted-test/audit gate, canonical bytecode reproduction and all
    validator mutations.

    Move only known generated outputs without overwrite into fresh bounded
    quarantines. Explicitly include `apps/web/next-env.d.ts` if Next recreates
    it. Then require ordinary and ignored-aware status both empty. Write
    `TASK6-I4-BUILDER-RECEIPT.md` bound to exact I4 and all input receipt hashes.
  </action>
  <verify>
    `parent(I4)=P10`; `P10..I4` is exactly the seven Task 2 paths; exact I4
    passes the complete matrix and mutations; plan count 11, partial/9 and
    authoritative completion zero; ignored residue negative probe fails closed;
    final ordinary/ignored-aware status empty; zero remotes.
  </verify>
  <acceptance_criteria>
    - TEST-CONTRACT names `apps/web/next-env.d.ts` and uses the exact
      ignored-aware status command.
    - The validator accepts only the defined 11-plan states and rejects the
      ignored next-env fixture.
    - I4 changes no product, ROADMAP or REQUIREMENTS path.
    - The new builder receipt explicitly supersedes only the stale cleanliness
      conclusion and preserves old receipts.
    - Every local matrix gate passes and all generated output is recoverably
      quarantined before cleanliness is claimed.
  </acceptance_criteria>
  <done>I4 is a truthful seven-file correction and evidence freeze over P10.</done>
  <commit>docs(phase-00): correct ignored-output evidence truth</commit>
</task>

<task type="auto" category="security" id="00-11-03">
  <name>Task 3: Independently review exact I4 and create R4</name>
  <files>.planning/phases/00-planning-truth-and-containment/00-SECURITY-REVIEW.md; external SECURITY-REVIEW-RECEIPT-v4.md</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-11-PLAN.md,
    .planning/TEST-CONTRACT.md,
    .planning/scripts/validate-planning.ps1,
    .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md,
    .planning/phases/00-planning-truth-and-containment/00-SECURITY-REVIEW.md,
    TASK6-I4-BUILDER-RECEIPT.md and every preserved Generation 3 receipt
  </read_first>
  <action>
    Use an adversarial reviewer distinct from the Plan 00-11 author/checker,
    I4 builder, V4 verifier and C4 owner. Freeze exact clean I4. Re-run the
    complete security-relevant matrix, validator/mutation suite, dependency
    audits, bytecode proof and targeted identity/issuance/web containment tests.
    Independently reproduce the ignored next-env negative probe and require
    ordinary and ignored-aware status to be empty after bounded quarantine.

    Write `SECURITY-REVIEW-RECEIPT-v4.md` bound to I4. Replace only the current
    internal SECURITY-REVIEW file with a transparent R4 report that identifies
    the V3 admission failure, declares old generated-cleanliness conclusions
    superseded, preserves all retained NO-GO boundaries, and states that the
    verdict binds I4 rather than its own future commit. Commit only that report
    as R4.
  </action>
  <verify>
    Full exact-I4 review evidence; zero unresolved critical/high, dependency,
    evidence-truth or false-success finding; `parent(R4)=I4`; `I4..R4` is only
    `00-SECURITY-REVIEW.md`; final ordinary/ignored-aware status empty.
  </verify>
  <acceptance_criteria>
    - Reviewer identity is distinct from author/checker, builder, verifier and
      admission owner.
    - Receipt and internal report name the exact V3 failure and I4 correction.
    - The ignored-file negative probe fails closed and the clean boundary has
      no ignored output.
    - R4 changes one repository file and does not claim Phase 0 or production
      readiness.
  </acceptance_criteria>
  <done>R4 independently accepts exact I4 for verification, not production.</done>
  <commit>docs(phase-00): review ignored-output evidence correction</commit>
</task>

<task type="auto" category="verify" id="00-11-04">
  <name>Task 4: Independently verify exact R4 and create V4</name>
  <files>.planning/phases/00-planning-truth-and-containment/00-VERIFICATION.md; external VERIFICATION-RECEIPT-v4.md</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-11-PLAN.md,
    .planning/TEST-CONTRACT.md,
    .planning/scripts/validate-planning.ps1,
    .planning/phases/00-planning-truth-and-containment/00-SECURITY-REVIEW.md,
    .planning/phases/00-planning-truth-and-containment/00-VERIFICATION.md,
    TASK6-I4-BUILDER-RECEIPT.md,
    SECURITY-REVIEW-RECEIPT-v4.md and every preserved Generation 3 receipt
  </read_first>
  <action>
    Use a verifier distinct from all prior roles. At exact clean R4, rerun the
    complete TEST-CONTRACT, acceptance matrix, mutation suite, bytecode proof,
    receipt-hash checks, allowlist/immutable comparisons and exact ignored-file
    negative probe. Record every criterion as exact pass, narrow supersession,
    retained NO-GO or pending C4 admission; any failure blocks the commit.

    Write `VERIFICATION-RECEIPT-v4.md` bound to R4. Replace only the current
    internal VERIFICATION file with a transparent V4 report that supersedes the
    stale generated-cleanliness conclusion, binds its verdict to R4 and keeps
    Phase 0 in_progress/NO-GO. Commit only that report as V4, then prove its
    parent and one-file diff.
  </action>
  <verify>
    Exact-R4 full matrix and typed criteria; receipt hashes; ignored-aware
    cleanliness before the report edit and after V4; `parent(V4)=R4`;
    `R4..V4` is only `00-VERIFICATION.md`; zero remotes.
  </verify>
  <acceptance_criteria>
    - Every mandatory exact-R4 gate passes with nonzero test discovery.
    - No stale evidence claim remains in the current reports or new receipts.
    - V4 is a one-file evidence-only child of R4.
    - Exact-V4 admission, Gitleaks and final topology remain explicitly pending.
  </acceptance_criteria>
  <done>V4 independently verifies exact R4 and is ready for final admission.</done>
  <commit>docs(phase-00): verify ignored-output evidence correction</commit>
</task>

<task type="auto" category="admission" id="00-11-05">
  <name>Task 5: Admit exact V4 as local-only C4</name>
  <files>none; external CANDIDATE-MANIFEST-v4.md only</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-11-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md,
    .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md,
    .planning/phases/00-planning-truth-and-containment/00-SECURITY-REVIEW.md,
    .planning/phases/00-planning-truth-and-containment/00-VERIFICATION.md,
    .planning/TEST-CONTRACT.md and all old and v4 external receipts
  </read_first>
  <action>
    Use a final-admission owner distinct from builder, reviewer and verifier.
    Freeze exact V4 and require ordinary and ignored-aware status to be empty
    before any test. Rerun the complete TEST-CONTRACT and all exact admission
    checks at V4. Quarantine every known generated output without overwrite,
    explicitly including a recreated `apps/web/next-env.d.ts`, and require both
    status forms empty again.

    Prove the V3->P10->I4->R4->V4 parent/path contracts, the full Plan 00-04
    through 00-11 union, product-source equality to I3, immutable ROADMAP and
    REQUIREMENTS, receipt hashes, silent strict/full-unreachable fsck, exactly
    one branch/worktree, zero remotes/tags/replace/grafts/alternates/shared/
    shallow/partial/promisor state and physical-equals-reachable objects. Run
    unchanged redacted Gitleaks 8.30.1 git/tree wrappers and the unchanged
    repository-artifact validator. Re-snapshot C2 without writing to it.
    Require its Git-object state and ordinary status to match the v2 manifest
    and prior snapshots, and require its ignored-file relative paths, sizes and
    hashes to match the read-only P10 inventory. Disclose its pre-existing
    ignored `apps/web/.npm-cache/` separately; do not describe C2 as
    filesystem-clean and do not clean or quarantine anything from C2.

    Only after every check passes, create external
    `CANDIDATE-MANIFEST-v4.md` and define `C4=V4`. Record commits/trees, plan and
    receipt hashes, scanner counts, tool versions, quarantine locations, matrix
    results and typed verdicts. State local-only, Phase 0 in_progress/NO-GO and
    every human/professional/hosted/production gate. Do not edit, stage or commit
    any repository file.
  </action>
  <verify>
    Exact-V4 full matrix; ignored-aware clean boundary before and after;
    zero safe scanner findings; exact lineage/allowlist/immutable/topology/object
    proofs; exact C2 Git and ignored-inventory equality; external manifest hash;
    no Git change.
  </verify>
  <acceptance_criteria>
    - Exact V4, not I4 or R4, passes every local test and admission command.
    - A recreated next-env.d.ts is recoverably quarantined and both status forms
      are empty before cleanliness is claimed.
    - CANDIDATE-MANIFEST-v4.md binds exact C4=V4 and no repository file changes.
    - The manifest distinguishes V4's ignored-aware clean state from C2's
      unchanged, explicitly inventoried ignored `.npm-cache` residue.
    - The manifest retains incident, hosted CI, Docker, legal, finance,
      accounting, tax, compliance, custody, external smart-contract audit and
      production gates as NO-GO conditions.
  </acceptance_criteria>
  <done>Exact clean V4 is admitted as local-only C4 without crossing any human, professional or production gate.</done>
</task>

</tasks>

<verification>

- [ ] V3 failure and non-admission are captured before residue is moved.
- [ ] P10 is an independently accepted plan-only child of V3.
- [ ] I4 is the exact seven-file corrective evidence child of P10.
- [ ] Plan count is 11, summary state is partial/9 and production completion is 0.
- [ ] Ignored next-env residue fails both the validator and final admission.
- [ ] Exact I4 and R4 pass their role-specific full matrices.
- [ ] R4 and V4 retain exact one-file report ancestry.
- [ ] Exact V4 passes the full matrix and repository admission.
- [ ] CANDIDATE-MANIFEST-v4.md binds C4=V4 without a Git change.
- [ ] Phase 0 and all requirements remain incomplete; release remains NO-GO.
- [ ] No remote, push, deployment, RPC, wallet, key, provider, funds, real
      assets, production configuration or irreversible action occurred.

</verification>

<success_criteria>

- The ignored-file blind spot is reproducibly detected, not merely cleaned once.
- All stale cleanliness claims are transparently superseded in new evidence.
- Every old commit and receipt remains preserved and hash-verifiable.
- The corrected candidate has complete independent local evidence ancestry.
- Financial and blockchain release gates remain fail closed: no mock/provider
  path, payment or mint finality, legal register, reconciliation, deployment,
  key-governance, external-audit or professional sign-off is inferred complete.

</success_criteria>

<failure_and_rollback>

- Any false claim, extra repository path, missing matrix gate, ignored residue,
  completed production state, mutation escape, stale evidence, scanner finding,
  role conflict or ambiguous authority is a hard stop.
- This is remediation cycle one for
  `EVIDENCE_TRUTH_IGNORED_GENERATED_PATH_OMISSION`. A repeated failure of that
  same root cause exhausts the two-cycle ceiling and requires fresh user review
  and a newly accepted plan.
- Never amend or overwrite C2, I3, R3, V3, their old receipts or an admitted C4.
- Quarantine only exact resolved paths beneath a fresh resolved OS-temporary
  root, require absent destinations and preserve hashes. Never use a broad,
  wildcard or repository-recursive delete.

</failure_and_rollback>

<output>

Repository outputs are limited to Plan 00-11, the seven-file I4 correction and
the one-file R4/V4 reports. External outputs use the deterministic v4 filenames
under the existing Generation 3 evidence directory.

</output>
