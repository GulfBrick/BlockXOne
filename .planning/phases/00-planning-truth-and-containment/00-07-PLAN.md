---
phase: 00-planning-truth-and-containment
plan: 07
type: execute
name: generation 3 July 2026 web advisory remediation
status: proposed
gap_closure: true
wave: 1
depends_on: []
autonomous: true
risk_class: critical
requirements: [BASE-03, BASE-04, BASE-06, BASE-07]
user_setup: []
execution_mode: controller-dispatched-serial-tasks
requirements_completion: forbidden
files_modified:
  - .planning/phases/00-planning-truth-and-containment/00-07-PLAN.md
  - apps/web/package.json
  - apps/web/package-lock.json
  - .planning/scripts/validate-planning.ps1
  - .planning/EVIDENCE-REGISTER.md
  - .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md
  - .planning/BLOCKERS.md
  - .planning/STATE.md
  - .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md
must_haves:
  truths:
    - "The July 2026 Next.js and Sharp high-severity advisory findings are remediated before I3."
    - "The web dependency change is limited to exact package manifest and lock closure; application source is unchanged."
    - "Next.js 15 remains on its maintenance line while Sharp 0.35.3 and bundled libvips are proven loadable."
    - "The current-phase plan set is exactly 00-01 through 00-07 while authoritative Phase 0 progress remains zero and NO-GO."
    - "The complete matrix passes at exact W5 before I3 and again at exact I3."
  artifacts:
    - "apps/web/package.json"
    - "apps/web/package-lock.json"
    - ".planning/scripts/validate-planning.ps1"
    - ".planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md"
  key_links:
    - "P6 is a plan-only child of D5; W5 is a two-file web-dependency child of P6."
    - "I3 is a six-file evidence child of W5; R3 and V3 retain one-file report ancestry."
    - "Plans 00-04 through 00-07 form the Generation 3 union allowlist."
---

# Plan 00-07: Generation 3 July 2026 web advisory remediation

## Objective

Remediate the newly published Next.js and Sharp high-severity advisories
without changing application source, weakening any audit or test gate, or
losing Generation 3's independent review ancestry.

## Approved local authority

The user approved continued execution of the controlled local delivery loop on
2026-07-23. That authority covers local planning, exact dependency resolution,
tests, evidence, independent review, independent verification and local
commits.

It does not authorize a remote, push, deployment, RPC, provider, wallet, key,
funds, real assets, production network, production configuration, professional
approval or irreversible action.

## Trigger and confirmed root cause

At exact clean D5
`78084349ff8b6385cc33e95900db6fd1094d68a0`, Plan 00-06 Task 3 passed:

- planning, repository-artifact, CI, Compose-policy and actionlint;
- all Go tests, vet, lint and gosec;
- govulncheck with zero reachable findings;
- web clean install, preflight 4/4, configuration 5/5, hermetic runner 21/21,
  root tests 92/92, package tests 92/92 and lint.

The full web `npm audit --audit-level=moderate` then exited 1 because advisory
data published after the earlier successful Generation 3 run reports:

- eight Next.js advisories affecting `next@15.5.20`, fixed in 15.5.21:
  `GHSA-m99w-x7hq-7vfj`, `GHSA-89xv-2m56-2m9x`,
  `GHSA-68g3-v927-f742`, `GHSA-4633-3j49-mh5q`,
  `GHSA-4c39-4ccg-62r3`, `GHSA-p9j2-gv94-2wf4`,
  `GHSA-q8wf-6r8g-63ch` and `GHSA-955p-x3mx-jcvp`; and
- reviewed `GHSA-f88m-g3jw-g9cj`, where Sharp versions below 0.35.0 inherit
  high-severity libvips vulnerabilities. The upstream advisory recommends
  current Sharp 0.35.3 with libvips 8.18.3.

This is advisory-database drift, not an application-source regression. The
official Next.js July 2026 security release directs maintenance-line users to
15.5.21. Sharp 0.35.3 requires Node >=20.9.0 and is compatible with pinned
Node 22.23.1.

## Isolated canonical resolution

An isolated package-manifest/lock simulation at:

`C:\Users\danie\AppData\Local\Temp\blockxone-web-audit-sim-9b322bccfa204bf8ac84c246c7dfa988`

proved the intended resolution:

- `next` 15.5.20 -> 15.5.21;
- `eslint-config-next` 15.5.20 -> 15.5.21;
- retain the existing scoped Next/PostCSS override and add a scoped
  `next -> sharp: 0.35.3` override because Next 15.5.21 still declares the
  vulnerable `sharp ^0.34.3` optional range;
- exactly one resolved Sharp package at 0.35.3;
- the matching Next env/ESLint-plugin/SWC closure at 15.5.21;
- the Sharp platform packages at 0.35.3, libvips packages at 1.3.2, and
  lock-selected `semver` 7.8.5;
- 41 changed package-lock entries, including the root entry, with no unrelated
  application dependency;
- clean `npm ci --ignore-scripts`; and
- full audit exit 0 with zero low, moderate, high or critical findings.

Candidate isolated SHA-256 values:

- `package.json`:
  `A9E464EC0663A5704B69AAE85F8881D9B7A8B232A563695D666C4818520CCEE8`
- `package-lock.json`:
  `9A8AF7F0CED0892B095F15E1E78922C737EC5AA861BE18249FF8D87340BBC7D5`

The isolated result is planning evidence, not an accepted repository change.

## Narrow supersession

This plan supersedes only the following Plan 00-06 assumptions:

- the exact current-phase plan set becomes `00-01-PLAN.md` through
  `00-07-PLAN.md`;
- `STATE.progress.total_plans` becomes 7;
- one recognized `00-04-SUMMARY.md` produces GSD artifact progress 14%, not
  17%, because the pinned tool applies `Math.round(1 / 7 * 100)`;
- accepted ancestry becomes
  `D5 -> P6 -> W5 -> I3 -> R3 -> V3`; and
- the six-file I3 path comparison is `W5..I3`, while `P6..W5` is exactly the
  two web package files.

Every other Plan 00-04, 00-05 and 00-06 control, retained blocker, non-claim,
role separation, immutable boundary and final NO-GO status remains binding.

ROADMAP, REQUIREMENTS, global GSD tooling and Plans 00-04 through 00-06 are
immutable. No `00-05-SUMMARY.md`, `00-06-SUMMARY.md` or
`00-07-SUMMARY.md` may be created.

<tasks>

<task type="auto" category="ops" id="00-07-01">
  <name>Task 1: Freeze and independently accept plan-only P6</name>
  <files>none; external GEN3-P6-WEB-ADVISORY-PLAN-ACCEPTANCE.md only</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-07-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-06-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-05-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-PLAN.md,
    .planning/TEST-CONTRACT.md,
    .planning/scripts/validate-planning.ps1,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    apps/web/package.json,
    apps/web/package-lock.json
  </read_first>
  <action>
    Execution owners: controller and independent plan checker.

    Define D5 as
    `78084349ff8b6385cc33e95900db6fd1094d68a0` with tree
    `24f04579481057d726c41c2fb67ac83a39d56d7c`. Commit this plan and no other
    path as P6 with message
    `docs(phase-00): accept July 2026 web advisory remediation`.

    Prove `parent(P6)=D5` and `git diff --name-only D5..P6` equals exactly
    `.planning/phases/00-planning-truth-and-containment/00-07-PLAN.md`.
    Prove one local branch, zero remotes/tags/replace refs/grafts/alternates,
    strict fsck, clean status and no generated repository path.

    Write external `GEN3-P6-WEB-ADVISORY-PLAN-ACCEPTANCE.md` under the
    established Generation 3 evidence directory. Bind D5/P6 commits and trees,
    plan and isolated candidate hashes, advisory identities, 2026-07-23 user
    authority, independent checker verdict, nine-path Plan 00-07 allowlist and
    union semantics with Plans 00-04 through 00-06.
  </action>
  <verify>
    Independent plan checker returns VERIFICATION PASSED; P6 is a clean
    plan-only descendant; receipt hash is recorded; no production action
    occurs.
  </verify>
  <acceptance_criteria>
    - `parent(P6)` is exact D5.
    - `D5..P6` changes only `00-07-PLAN.md`.
    - Plan 00-07 names exactly nine repository paths.
    - ROADMAP, REQUIREMENTS, prior plans and global GSD files are absent from
      the allowlist.
    - Receipt states local-only, Phase 0 in_progress, seven total plans, zero
      production progress and NO-GO.
    - No unapproved summary exists.
  </acceptance_criteria>
  <done>P6 is an independently checked plan-only descendant with exact scope.</done>
  <commit>docs(phase-00): accept July 2026 web advisory remediation</commit>
</task>

<task type="auto" category="code" id="00-07-02">
  <name>Task 2: Commit the canonical two-file web dependency remediation as W5</name>
  <files>apps/web/package.json, apps/web/package-lock.json</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-07-PLAN.md,
    .planning/TEST-CONTRACT.md,
    apps/web/package.json,
    apps/web/package-lock.json,
    apps/web/next.config.mjs
  </read_first>
  <action>
    Execution owner: web dependency remediator who did not check this plan.

    At exact clean P6, use only Node 22.23.1 and npm 10.9.8. Update
    `package.json` exactly as proven by the isolated candidate:

    - `next` -> 15.5.21;
    - `eslint-config-next` -> 15.5.21; and
    - under the existing `overrides.next` object, retain
      `postcss: 8.5.10` and add `sharp: 0.35.3`.

    Generate `package-lock.json` canonically with an exact lock-only install
    using `--ignore-scripts`, `--no-audit` and `--fund=false`. Require the two
    candidate files to match the retained isolated SHA-256 values exactly.
    Reject any additional manifest, dependency, source or generated-file
    change.

    Run a clean `npm ci --ignore-scripts`. Require exactly one Sharp resolution
    at 0.35.3, Next/env/SWC at 15.5.21, matching eslint-config/plugin at
    15.5.21, and no vulnerable nested Sharp.

    Run an in-memory Sharp native smoke that requires the module, asserts
    `sharp.versions.sharp == 0.35.3`, asserts bundled libvips is at least
    8.18.3, creates a tiny image buffer, reads its metadata and verifies the
    expected dimensions without writing a repository file.

    Run all web preflight/config/runner/root/package suites with nonzero counts,
    lint, full and runtime audits, production build and live containment probe.
    Both audits must exit zero with zero findings. The build must retain the
    exact expected route count and production containment.

    Commit exactly the two package files as W5 with message
    `fix(web): remediate July 2026 advisories`.
  </action>
  <verify>
    `parent(W5)=P6`; `P6..W5` is exactly package.json/package-lock.json;
    candidate hashes match; clean install, native Sharp smoke, all web gates,
    both audits, build and containment pass at exact W5; status clean, remotes
    zero and generated output quarantined recoverably.
  </verify>
  <acceptance_criteria>
    - No application or test source change.
    - Exact Next and eslint-config-next are 15.5.21.
    - Exactly one Sharp 0.35.3 loads with bundled libvips >=8.18.3.
    - Preflight 4/4, config 5/5, runner 21/21 and both 92/92 suites pass.
    - Lint, full/runtime audits, build and live containment pass.
    - W5 changes exactly two repository paths and ends clean.
  </acceptance_criteria>
  <done>W5 is a clean two-file web dependency remediation with zero npm audit findings.</done>
  <commit>fix(web): remediate July 2026 advisories</commit>
</task>

<task type="auto" category="docs" id="00-07-03">
  <name>Task 3: Re-run the complete matrix, freeze six-file I3 and prove exact I3</name>
  <files>
    .planning/scripts/validate-planning.ps1,
    .planning/EVIDENCE-REGISTER.md,
    .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md,
    .planning/BLOCKERS.md,
    .planning/STATE.md,
    .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-07-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-06-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-05-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-PLAN.md,
    .planning/TEST-CONTRACT.md,
    .planning/scripts/validate-planning.ps1,
    .planning/EVIDENCE-REGISTER.md,
    .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md,
    .planning/BLOCKERS.md,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Execution owner: validator/evidence builder who authored neither this plan
    nor its independent verdict.

    At exact clean W5, run every TEST-CONTRACT and exact bytecode-evidence
    command with pinned toolchains. Missing, skipped or zero-test suites are
    fatal. Include the Plan 00-07 Sharp native smoke in the web group.

    Apply the Plan 00-05 path-specific validator design with these final
    supersessions:

    1. Require the exact plan set `00-01-PLAN.md` through `00-07-PLAN.md`.
    2. Recognize only `00-04-SUMMARY.md`; reject `SUMMARY.md`, every other
       summary, case variant and additional summary.
    3. Require pinned `init milestone-op.completed_phases` to equal
       `summaryPresent` 0 or 1 and label it only as a summary-bearing-phase
       artifact counter.
    4. Require Phase 0 from `roadmap analyze` to have `plan_count=7`,
       `summary_count=summaryPresent`, `completed_phases=0`,
       `current_phase='0'`, and:
       - without summary: `disk_status='planned'`, artifact progress 0;
       - with summary: `disk_status='partial'`, artifact progress 14.
    5. Require exact builder-summary metadata/non-claims from Plan 00-05:
       plan 04, implementation frozen for independent review, NO-GO, no
       completed requirements, Phase 0 in_progress, builder evidence only and
       Tasks 7-9 mandatory.
    6. Require STATE in_progress, completed phases/plans zero and total plans 7.
    7. Require unchecked/in-progress Phase 0 ROADMAP status.
    8. Require exactly 89 unchecked, unique requirement IDs and zero checked.

    Mutation-test wrong/missing/extra/case-variant plan and summary names,
    wrong summary metadata, completed STATE, completed ROADMAP and
    checked/duplicate/missing requirements. The validator must accept exact
    no-summary W5 and exact-one-summary I3 states.

    Recreate only the five builder-evidence files. Record both advisory-drift
    hard stops, P5/D5/P6/W5 ancestry, exact Go and web dependency deltas,
    complete W5/I3 results, counts, warnings, hashes and every retained NO-GO
    blocker. Set STATE total_plans to 7 and all completed values to zero.
    ROADMAP and REQUIREMENTS remain byte-identical.

    Commit exactly the validator and five evidence paths as I3 with message
    `docs(phase-00): record generation 3 local matrix`.

    At exact clean I3, rerun the complete TEST-CONTRACT, Sharp smoke and
    bytecode validation. Write external `TASK6-I3-BUILDER-RECEIPT.md` bound to
    P4/P5/D5/P6/W5/I3 commits/trees, all commands/exits/counts/warnings/module
    selections/hashes and generated-path absence. Quarantine exact generated
    outputs beneath a new validated OS-temp directory; never recursively delete
    or overwrite.
  </action>
  <verify>
    Full matrix passes at exact W5 and exact I3; validator passes exact W5/I3
    states and mutations; `W5..I3` is exactly six allowed paths; immutable
    files match; status clean and remotes zero.
  </verify>
  <acceptance_criteria>
    - W5 and I3 mandatory commands all exit zero with nonzero tests.
    - `W5..I3` changes exactly the validator and five evidence paths.
    - Validator accepts only 0-summary/0%-planned W5 and
      1-summary/14%-partial I3 artifact states.
    - Authoritative STATE remains in_progress with total_plans 7 and every
      completed value zero.
    - ROADMAP and REQUIREMENTS are byte-identical to D5.
    - Summary is builder-only, requirements-completed empty and NO-GO.
    - Exact I3 is clean, remote-free and contains no generated path.
  </acceptance_criteria>
  <done>I3 is a clean six-file evidence freeze over remediated Go and web dependencies.</done>
  <commit>docs(phase-00): record generation 3 local matrix</commit>
</task>

<task type="auto" category="verify" id="00-07-04">
  <name>Task 4: Resume independent R3, V3 and local C3 admission</name>
  <files>none beyond Plan 00-04 Task 7 and Task 8 one-file reports</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-07-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-06-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-05-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md,
    .planning/TEST-CONTRACT.md,
    .planning/BLOCKERS.md
  </read_first>
  <action>
    Execution owners remain three distinct agents who authored neither the
    dependency/evidence tree nor each other's report.

    Resume Plan 00-04 Tasks 7-9 with Plans 00-05 through 00-07 binding:

    - Task 7 reviews exact I3 and commits only `00-SECURITY-REVIEW.md` as R3;
      require `parent(R3)=I3`.
    - Task 8 reruns the complete matrix at exact R3 and commits only
      `00-VERIFICATION.md` as V3; require `parent(V3)=R3`.
    - Task 9 reruns the complete matrix and final repository admission at exact
      V3, writes only external `CANDIDATE-MANIFEST-v3.md`, and defines C3=V3.

    Use the union of Plans 00-04 through 00-07. Source/test content at R3/V3
    must equal I3 source/test content; only the authorized reports may differ.
    Re-snapshot immutable C2 and retain Phase 0 in_progress, zero completed
    requirements/plans/phases, total plans 7 and release NO-GO.
  </action>
  <verify>
    Independent identities; exact I3/R3/V3 binding; one-file report diffs;
    exact-V3 complete matrix; union allowlist and immutable equality; strict
    topology/object/scanner checks; C2 equality; zero remotes.
  </verify>
  <acceptance_criteria>
    - Task 7 has no unresolved critical/high, dependency or false-success
      finding.
    - `I3..R3` is only SECURITY-REVIEW and `R3..V3` only VERIFICATION.
    - Exact V3 passes every TEST-CONTRACT and repository-admission command.
    - External C3 manifest binds P3, D4, P4, P5, D5, P6, W5, I3, R3, V3 and
      C3=V3.
    - Manifest states local candidate only, Phase 0 in_progress/NO-GO and no
      production action.
  </acceptance_criteria>
  <done>Generation 3 is independently reviewed, verified and locally admitted under the checked union boundary.</done>
</task>

</tasks>

<verification>

- [ ] P6 is a plan-only child of exact D5.
- [ ] W5 is a two-file web-dependency child of P6.
- [ ] Next.js and Sharp advisory findings are absent at W5, I3, R3 and V3.
- [ ] Sharp 0.35.3 and bundled libvips >=8.18.3 pass native smoke.
- [ ] Current phase contains exactly seven plans and one permitted summary.
- [ ] GSD 0/1 and 0/14 artifact counters are never called production progress.
- [ ] STATE remains in_progress with seven total and zero completed plans.
- [ ] ROADMAP Phase 0 and all 89 requirements remain unchecked.
- [ ] Complete matrix passes at W5, I3, R3 and exact V3.
- [ ] I3/R3/V3 parent and exact-path contracts pass.
- [ ] No unapproved summary exists.
- [ ] No remote, push, deployment, RPC, provider, wallet, key, funds, real
      assets, production configuration or irreversible action occurs.

</verification>

<success_criteria>

- P6 has an independent plan-checker verdict and external acceptance receipt.
- W5 contains only the canonical Next/Sharp package resolution.
- Exact I3 contains complete builder evidence without claiming independent
  review or production completion.
- Exact R3 and V3 retain independent one-file evidence ancestry.
- Exact C3=V3 is admitted locally while Phase 0 remains in_progress and NO-GO.

</success_criteria>

<failure_and_rollback>

- Any extra package/path/version, product-source change, extra summary,
  completed checkbox/status, audit finding, native-load failure, matrix
  failure, stale evidence or non-independent verdict is a hard stop.
- One web-dependency remediation cycle is permitted. A repeat of the same root
  cause requires another checked plan.
- Never amend D5, P6, W5, I3, R3, V3 or admitted C3.
- Preserve failed attempts and receipts; move only exact validated generated
  paths to recoverable quarantine.

</failure_and_rollback>

<output>

Repository outputs are limited to this plan, exact two-file web dependency
remediation, the six-file I3 freeze and original Plan 00-04 one-file
reviewer/verifier reports. External receipts remain under:

`C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721`.

</output>
