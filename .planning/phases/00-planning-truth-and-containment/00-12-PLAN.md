---
phase: 00-planning-truth-and-containment
plan: 12
type: execute
name: fail-closed native command execution and evidence provenance correction
status: proposed
gap_closure: true
wave: 2
depends_on: [00-11]
autonomous: true
risk_class: critical
requirements: [BASE-03, BASE-04, BASE-06, BASE-07]
user_setup: []
execution_mode: controller-dispatched-serial-tasks
requirements_completion: forbidden
files_modified:
  - .planning/phases/00-planning-truth-and-containment/00-12-PLAN.md
  - .planning/TEST-CONTRACT.md
  - .planning/scripts/invoke-phase0-matrix.ps1
  - .planning/scripts/test-phase0-matrix-runner.ps1
  - .planning/scripts/validate-planning.ps1
  - .planning/BLOCKERS.md
  - .planning/EVIDENCE-REGISTER.md
  - .planning/STATE.md
  - .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md
  - .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md
  - .planning/phases/00-planning-truth-and-containment/00-SECURITY-REVIEW.md
  - .planning/phases/00-planning-truth-and-containment/00-VERIFICATION.md
must_haves:
  truths:
    - "I4 remains preserved but is not independently accepted because the attempted R4 review reproduced native-command exit masking in the Phase 0 TEST-CONTRACT."
    - "Every mandatory native command is executed through one fail-closed runner that captures and checks that command's exit code before any later native command can overwrite it."
    - "Generated-output quarantine rejects a reparse point at the component root, any existing ancestor or the terminal source before any move."
    - "The immutable I4 builder receipt's four stale mutation-root paths are explicitly corrected by new evidence without amending the old receipt."
    - "Every new receipt identifies its canonical agent task path and role-separation matrix; no legal-person identity is inferred."
    - "Plan 00-12 raises the GSD artifact count to twelve while authoritative production completion remains zero."
    - "P11, I5, R5, V5 and C5 preserve distinct plan acceptance, builder, reviewer, verifier and final-admission roles."
    - "No product behavior, product source, ROADMAP, requirement checkbox or professional or production gate changes."
  artifacts:
    - ".planning/phases/00-planning-truth-and-containment/00-12-PLAN.md"
    - ".planning/scripts/invoke-phase0-matrix.ps1"
    - ".planning/scripts/test-phase0-matrix-runner.ps1"
    - ".planning/TEST-CONTRACT.md"
    - ".planning/scripts/validate-planning.ps1"
  key_links:
    - "I4 failed R4 review -> external v4 failure receipt -> P11 accepted corrective plan."
    - "P11 -> I5 -> R5 -> V5 preserves independently checked evidence ancestry."
    - "Exact V5 -> external CANDIDATE-MANIFEST-v5.md -> local-only C5=V5."
    - "TEST-CONTRACT -> one runner command -> exact checked native-command inventory."
    - "Runner tests -> PowerShell AST and executable mutations -> runner enforcement."
    - "Planning validator -> exact nine-path staged status contract -> I5 scope freeze."
---

# Plan 00-12: Fail-closed native command execution and evidence provenance correction

## Objective

<objective>
Correct the attempted R4 review failure without amending, overwriting or
silently reclassifying I4 or any earlier receipt.

Replace the copy/paste Phase 0 native-command sequence with a repository-owned,
testable PowerShell runner. The runner must check each native process result
immediately, preserve the first command failure through cleanup, report any
cleanup failure separately, and refuse a success result when any mandatory
command failed.

Correct the immutable I4 builder receipt's four stale absolute mutation-root
locations in new evidence. The old receipt remains hash-verifiable historical
evidence; its incorrect path statements are never repeated as current paths.
</objective>

This is a local-only controlled correction inside Phase 0. It does not
authorize a remote, push, pull, fetch, deployment, live RPC, provider, wallet,
signer, key, funds, real assets, production network, production configuration,
professional approval or irreversible action.

Phase 0 remains `in_progress`; all 89 requirements remain unchecked; release
remains `NO-GO`.

## Trigger and typed root causes

The attempted R4 reviewer reproduced this sequence in PowerShell:

1. a native process exited `7`;
2. a later native process exited `0`; and
3. the containing PowerShell process exited `0`.

The I4 TEST-CONTRACT checks `$LASTEXITCODE` only after its final `git status`
command. Its mandatory `pwsh`, `actionlint`, Go, npm and Docker invocations do
not perform an immediate per-command exit assertion and do not establish
`$PSNativeCommandUseErrorActionPreference`. A later successful command can
therefore erase an earlier native failure from the observable contract result.

The blocking typed root cause is:

`TEST_CONTRACT_NATIVE_EXIT_MASKING`

The same review also confirmed a separate evidence-provenance defect:

`EVIDENCE_PROVENANCE_PATH_TRANSCRIPTION_ERROR`

`TASK6-I4-BUILDER-RECEIPT.md` lists four mutation roots beneath:

`C:\Users\danie\AppData\Local\Temp\blockxone-i4-matrix-9775e9f3256e4c8083be6af95e10c968`

Those four paths do not exist there. The retained directories exist beneath:

`C:\Users\danie\AppData\Local\Temp\blockxone-i4-staged-fixtures-be2445c0c6104e9ba79742e0e328d192`

The exact-I4 and no-summary matrix logs also record the correct retained roots
and the 53/39 passing counts. This means the mutation evidence is retained, but
the immutable receipt's four printed absolute locations are stale. New
evidence must state both facts and bind the correct paths and log hashes.

The final v4 failure receipt is immutable at SHA-256
`0D9B3DF8697646FBB4747BD12B3521DA39E898C286593809CF9C3492176DD7B5`.
P11 and every later receipt must bind these exact corrections:

| Absent path printed by I4 receipt | Correct retained path | Cases |
|---|---|---:|
| `C:\Users\danie\AppData\Local\Temp\blockxone-i4-matrix-9775e9f3256e4c8083be6af95e10c968\mutations-no-summary-final-045661aff3334d7d9581536d0bfbbf4e` | `C:\Users\danie\AppData\Local\Temp\blockxone-i4-staged-fixtures-be2445c0c6104e9ba79742e0e328d192\mutations-no-summary-final-045661aff3334d7d9581536d0bfbbf4e` | 39 |
| `C:\Users\danie\AppData\Local\Temp\blockxone-i4-matrix-9775e9f3256e4c8083be6af95e10c968\mutations-summary-final-f8461c82d4204007a4a76a084d9a846a` | `C:\Users\danie\AppData\Local\Temp\blockxone-i4-staged-fixtures-be2445c0c6104e9ba79742e0e328d192\mutations-summary-final-f8461c82d4204007a4a76a084d9a846a` | 53 |
| `C:\Users\danie\AppData\Local\Temp\blockxone-i4-matrix-9775e9f3256e4c8083be6af95e10c968\mutations-exact-i4-post-quarantine-e321d48a9f8f4a2eb60d713796a7da68` | `C:\Users\danie\AppData\Local\Temp\blockxone-i4-staged-fixtures-be2445c0c6104e9ba79742e0e328d192\mutations-exact-i4-post-quarantine-e321d48a9f8f4a2eb60d713796a7da68` | 53 |
| `C:\Users\danie\AppData\Local\Temp\blockxone-i4-matrix-9775e9f3256e4c8083be6af95e10c968\mutations-post-i4-no-summary-f86f20d970e74728ae0a01e27a6ef1d9` | `C:\Users\danie\AppData\Local\Temp\blockxone-i4-staged-fixtures-be2445c0c6104e9ba79742e0e328d192\mutations-post-i4-no-summary-f86f20d970e74728ae0a01e27a6ef1d9` | 39 |

The eight original retained log bindings are:

| Log | Bytes | SHA-256 |
|---|---:|---|
| `primary staged root\summary-final.out.log` | 5,749 | `5466E74BB4AA48BB27639C004B83E0B9CD8E8352A09C4E81727B11C69413331C` |
| `primary staged root\summary-final.err.log` | 807,798 | `94868F441AD8F74341998FE23364C62C8664F9E6C74A99A96691EFF727EFD5ED` |
| `primary staged root\no-summary-final.out.log` | 4,223 | `6DD7DE49449FAAE73AA25DF023CF8A4DB162D583C0FF3F13E974A61A097E300A` |
| `primary staged root\no-summary-final.err.log` | 588,179 | `365835159495F68FF2C987703857F57EB835DAD8CDC61D932D488495314F6196` |
| `matrix root\exact-i4-mutations.out.log` | 5,782 | `EC46276750685830F7B43226156DFDD1302A2CACB7C8DB8ABDCD7BB23AC2F77F` |
| `matrix root\exact-i4-mutations.err.log` | 807,798 | `94868F441AD8F74341998FE23364C62C8664F9E6C74A99A96691EFF727EFD5ED` |
| `matrix root\post-i4-no-summary-mutations.out.log` | 4,229 | `7A3EAFA8BB95E61E8D7B58D11C882AECE5DEE1256FB82258AF5E0A272CD3FA62` |
| `matrix root\post-i4-no-summary-mutations.err.log` | 588,179 | `365835159495F68FF2C987703857F57EB835DAD8CDC61D932D488495314F6196` |

The reviewer also found that the I4 quarantine helper validates only the
terminal source entry for a reparse-point attribute. The nested
`apps/web/node_modules/.vite` entry is considered before its
`apps/web/node_modules` ancestor. A junction at the ancestor can therefore
redirect the nested path before the parent junction is rejected. There is no
ancestor-junction negative fixture.

That typed root cause is:

`GENERATED_OUTPUT_ANCESTOR_REPARSE_ESCAPE`

Finally, the immutable I4 receipt states only `Role: I4 builder only`; it does
not record the canonical agent task identity. The controller transcript
distinguishes the roles, but the external evidence packet does not make that
separation independently auditable.

That typed evidence-governance root cause is:

`AGENT_ROLE_PROVENANCE_OMISSION`

The topology subreview also found that the immutable I4 receipt says no other
repository path was edited while separately recording recoverable moves of six
unreachable loose files from `.git/objects`. The accurate scope is that no
other tracked working-tree path was edited; the object-store moves were
repository-metadata mutations even though all six objects were proven
unreferenced and preserved.

That typed evidence-scope root cause is:

`EVIDENCE_SCOPE_WORDING_OVERSTATEMENT`

I4 is preserved evidence of the stopped R4 attempt. It is not R4, V4 or C4 and
must never be described as independently accepted or admitted.

## Corrective lineage and immutable boundary

| Boundary | Exact contract |
| --- | --- |
| Stopped review input | `I4 = ff24f9d79f4a02e3cac054283140a2ed3226d5c5`; preserved and not accepted by R4 |
| Failed review evidence | external `SECURITY-REVIEW-FAILURE-RECEIPT-v4.md`; no Git change |
| Corrective plan | `P11`; parent I4; changes only this Plan 00-12 |
| Corrected builder freeze | `I5`; parent P11; changes exactly the nine Task 2 paths |
| Independent review | `R5`; parent I5; changes only `00-SECURITY-REVIEW.md` |
| Independent verification | `V5`; parent R5; changes only `00-VERIFICATION.md` |
| Local candidate | `C5 = V5`; external manifest only; no Git change |
| Immutable baseline | C2 and its v2 manifest remain byte-for-byte unchanged |
| Retry ceiling | This is remediation cycle one for each typed root cause above |

Never amend or overwrite I3, R3, V3, P10, I4 or any old external receipt.
Preserve the old statements as historical evidence and explicitly supersede
the native-exit success implication and four stale path statements in the new
failure, acceptance, builder, review, verification and admission evidence.
The same new evidence must state the canonical Codex task path for each agent
role and an explicit not-the-same-role matrix. A canonical task path is
workflow provenance, not proof of a legal person's identity, employment,
licence, independence or professional approval.

For Plan 00-12, the canonical author task path is `/root` and the independent
checker task path is `/root/gen3_plan12_checker`. P11 must bind both exact
strings. I5, R5, V5 and C5 receipts must append their canonical task path and
prove all six task paths are pairwise distinct. Reusing a task path for any two
of author, checker, builder, reviewer, verifier or admission owner is a hard
stop.

Repository changes from I4 through V5 are limited to this plan's
`files_modified` allowlist. Product source, product tests, dependency locks,
CI, Compose, contracts, ROADMAP.md, REQUIREMENTS.md, Plans 00-01 through 00-11,
the repository-artifact validator and all other proof scripts are immutable.

The external evidence directory remains:

`C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721`

## Required runner contract

The new `.planning/scripts/invoke-phase0-matrix.ps1` is the sole executable
entry point for the complete Phase 0 local matrix.

It must:

1. require PowerShell 7 or later and set `$ErrorActionPreference = 'Stop'`;
2. enable `$PSNativeCommandUseErrorActionPreference` when the variable is
   available, while still enforcing an explicit immediate exit-code check;
3. require the exact pinned Node executable, npm CLI file and Go executable as
   mandatory path parameters; verify their absolute paths, leaf-file type and
   SHA-256 before use; resolve every remaining tool as an external application,
   not an alias, function or script shadow; and record every executable/file
   used;
4. route every `pwsh`, `actionlint`, `go`, `golangci-lint`, `gosec`,
   `govulncheck`, `node`, `npm`, `docker` and `git` process invocation through
   one `Invoke-NativeChecked` function;
5. capture the exact native exit code immediately after that invocation and
   throw a labeled error containing the tool label and exit code when nonzero;
6. never invoke a second native command between the target process and its
   exit-code capture;
7. verify the pinned Node 22.23.1, npm 10.9.8, Go 1.26.5, actionlint 1.7.12,
   golangci-lint 2.12.2, gosec 2.25.0 and govulncheck 1.6.0 identities before
   accepting their later results;
8. preserve the exact prior process-environment presence/value of
   `GOTOOLCHAIN`, set it to `local` before any Go-backed identity or matrix
   command, assert the in-process value and checked `go env GOTOOLCHAIN` output
   are exactly `local`, and restore the prior presence/value even on failure;
9. invoke `test-phase0-matrix-runner.ps1` as the first checked, labeled native
   step after tool identity validation; TEST-CONTRACT itself must expose only
   the one runner command and no second outer native command;
10. run every frozen TEST-CONTRACT validation, Go, web, contract, audit and
   Compose-render command with the same scopes and arguments;
11. set the web fixture environment only for the bounded web segment and restore
   the exact prior process-environment presence and values;
12. restore every pushed location even after command failure;
13. reject a reparse point at each component root, each existing path component
    between the root and source, and the terminal source before any move;
14. avoid a separately moved nested output when moving its validated parent
    already preserves that output, including `node_modules/.vite`;
15. require source and quarantine roots to be on the same filesystem volume
    and use no-overwrite file/directory move primitives that fail rather than
    nesting into or replacing an existing destination;
16. reject a reparse point at the resolved temporary root, quarantine root or
    any destination ancestor;
17. quarantine only the enumerated generated paths into a unique absent
    resolved OS-temporary directory using literal, bounded moves;
18. explicitly include `apps/web/next-env.d.ts`;
19. capture the first matrix failure, execute bounded cleanup, and then emit a
    nonzero result that preserves the original command label and exit code;
20. if cleanup also fails, preserve both the primary and cleanup diagnostics
    and still return nonzero;
21. continue attempting the remaining bounded cleanup entries after one cleanup
    entry fails, collect each cleanup diagnostic, and never report success;
22. perform the exact ignored-aware Git status check through the same checked
    native wrapper after a successful matrix and cleanup;
23. refuse success when ordinary, untracked or ignored residue remains; and
24. emit a deterministic success marker only after all commands, cleanup and
    final ignored-aware status have succeeded.

The runner may support a bounded self-test mode, but that mode must be
syntactically distinct from the real matrix path, must never report a matrix
pass, and must not provide a switch that skips a mandatory real-matrix command.

## Frozen checked-command inventory

The runner and its AST tests must share this plan-owned inventory, not a list
invented by the implementation. Labels, order, working directory, logical
arguments and environment boundaries are exact. A missing, duplicate, reordered
or extra production label is a failure.

Before the matrix, resolve and record the application path and SHA-256 for
PowerShell, Node, npm CLI, Go, actionlint, golangci-lint, gosec, govulncheck,
Docker and Git. Check these native identity labels through
`Invoke-NativeChecked`:

The following inputs are mandatory because ambient `PATH` currently resolves a
different Node/npm/Go toolchain and is not an admissible resolver:

| Runner parameter | Exact leaf file | Required SHA-256 |
|---|---|---|
| `-NodeExecutable` | `C:\Users\danie\AppData\Local\npm-cache\_npx\d295cebdb7c54afe\node_modules\node\bin\node.exe` | `F8D162C0641DCEE512132F3BCF8A68169C7ECB852EFD8E1A46C9FEC5A0F469ED` |
| `-NpmCli` | `C:\Users\danie\AppData\Local\npm-cache\_npx\d8f2e63c6145eb9d\node_modules\npm\bin\npm-cli.js` | `8E5F6F3429F8CDBE693CDC29904E9D5A7B127A494BD15C804BD54C7403BFCBE7` |
| `-GoExecutable` | `C:\Users\danie\go\pkg\mod\golang.org\toolchain@v0.0.1-go1.26.5.windows-amd64\bin\go.exe` | `D6A71E6DC9806CB3F1FCB817D562175C5A7B1505FC3F6638299648E0B9F0F74F` |

The runner must not fall back to `Get-Command`, `PATH`, a shim or a package
manager for these three inputs. Every logical npm command below is a checked
native invocation of the exact `-NodeExecutable` with exact `-NpmCli` as its
first argument. Every logical Go command uses exact `-GoExecutable`.

| Order | Label | Exact logical command / required identity |
|---:|---|---|
| T01 | `tool.node.version` | exact `-NodeExecutable --version`; exactly `v22.23.1` |
| T02 | `tool.npm.version` | exact `-NodeExecutable -NpmCli --version`; exactly `10.9.8` |
| T03 | `tool.go.version` | with scoped process `GOTOOLCHAIN=local`, exact `-GoExecutable version`; output must contain exactly `go1.26.5` tool identity; do not claim the output prints `GOTOOLCHAIN` |
| T04 | `tool.go.toolchain-env` | with scoped process `GOTOOLCHAIN=local`, exact `-GoExecutable env GOTOOLCHAIN`; output exactly `local` and in-process `$env:GOTOOLCHAIN` exactly `local` |
| T05 | `tool.actionlint.version` | `actionlint -version`; exactly `v1.7.12` |
| T06 | `tool.golangci-lint.version` | `golangci-lint version`; exactly `2.12.2` |
| T07 | `tool.gosec.module` | with scoped process `GOTOOLCHAIN=local`, `go version -m <resolved-gosec-application>`; module `github.com/securego/gosec/v2` exactly `v2.25.0` |
| T08 | `tool.govulncheck.version` | with scoped process `GOTOOLCHAIN=local`, `govulncheck -version`; scanner exactly `govulncheck@v1.6.0` |
| T09 | `tool.docker.version` | `docker --version`; record result, no new version approval inferred |
| T10 | `tool.git.version` | `git --version`; record result, no new version approval inferred |
| T11 | `runner.tests` | resolved `pwsh -NoProfile -File .\.planning\scripts\test-phase0-matrix-runner.ps1`; must pass before the real matrix |

PowerShell must also verify its in-process version is at least 7 and record
`[Environment]::ProcessPath`; this is not a second child process. npm commands
must use the exact pinned Node/npm-CLI pair whose checked version is 10.9.8.
The runner must preserve the prior `GOTOOLCHAIN` presence/value before T03,
keep its scoped value exactly `local` through matrix label 11
`go.govulncheck`, and restore the prior presence/value immediately afterward or
from `finally` on any earlier failure.

The exact checked matrix is:

| Order | Label | Working directory | Exact logical command |
|---:|---|---|---|
| 01 | `planning.validate` | repository | `pwsh -NoProfile -File .\.planning\scripts\validate-planning.ps1 -ResidueMode CleanCandidate` |
| 02 | `artifacts.validate` | repository | `pwsh -NoProfile -File .\.planning\scripts\validate-repository-artifacts.ps1` |
| 03 | `ci-policy.validate` | repository | `pwsh -NoProfile -File .\.planning\scripts\validate-ci-policy.ps1` |
| 04 | `compose-policy.validate` | repository | `pwsh -NoProfile -File .\.planning\scripts\validate-production-compose.ps1` |
| 05 | `workflows.actionlint` | repository | `actionlint -no-color` |
| 06 | `go.module-verify` | repository | `go mod verify` |
| 07a | `go.test` | repository | `go test -count=1 .\cmd\... .\internal\... .\scripts\...` |
| 07b | `go.test.discovery` | repository | `go test -count=1 -json .\cmd\... .\internal\... .\scripts\...`; parse every JSON line and require the expected nonzero test/package disposition |
| 08 | `go.vet` | repository | `go vet .\cmd\... .\internal\... .\scripts\...` |
| 09 | `go.lint` | repository | `golangci-lint run --timeout 5m .\cmd\... .\internal\... .\scripts\...` |
| 10 | `go.gosec` | repository | `gosec -tests -severity medium -confidence medium .\cmd\... .\internal\...` |
| 11 | `go.govulncheck` | repository | `govulncheck .\cmd\... .\internal\... .\scripts\...` |
| 12 | `web.install` | repository | `npm ci --prefix apps/web` |
| 13 | `web.sharp-smoke` | repository | checked Node inline probe over the installed `apps/web` Sharp package using the exact script below; require the exact `SHARP_SMOKE_PASS` marker |
| 14 | `web.preflight-tests` | repository | `npm run test:preflight --prefix apps/web` |
| 15 | `web.config-tests` | repository | `npm run test:config --prefix apps/web` |
| 16 | `web.runner-tests` | repository | `npm run test:runner --prefix apps/web` |
| 17 | `web.unit.repo-root` | repository | `npm test --prefix apps/web -- --run` |
| 18 | `web.unit.package-root` | `apps/web` | `npm test -- --run` |
| 19 | `web.lint` | `apps/web` | `npm run lint` |
| 20 | `web.audit.full.pre-build` | `apps/web` | `npm audit --audit-level=moderate` |
| 21 | `web.audit.runtime.pre-build` | `apps/web` | `npm audit --omit=dev --audit-level=moderate` |
| 22 | `web.build` | `apps/web` | `npm run build` |
| 23 | `web.production-containment` | `apps/web` | `npm run test:production-containment` |
| 24 | `web.audit.full.post-containment` | `apps/web` | `npm audit --audit-level=moderate` |
| 25 | `web.audit.runtime.post-containment` | `apps/web` | `npm audit --omit=dev --audit-level=moderate` |
| 26 | `contracts.install` | `contracts` | `npm ci --ignore-scripts` |
| 27 | `contracts.preflight-tests` | `contracts` | `npm run test:preflight` |
| 28 | `contracts.compile` | `contracts` | `npm run compile` |
| 29 | `contracts.typecheck` | `contracts` | `npm run typecheck` |
| 30 | `contracts.test.full` | `contracts` | `npm test` |
| 31 | `contracts.test.identity` | `contracts` | `npm test -- test/IdentityRegistry.test.ts`; require 16 tests |
| 32 | `contracts.test.token` | `contracts` | `npm test -- test/BXOSecurityToken.test.ts`; require 36 tests |
| 33 | `contracts.test.factory` | `contracts` | `npm test -- test/BXOSecurityTokenFactory.test.ts`; require 5 tests |
| 34 | `contracts.audit.runtime` | `contracts` | `npm audit --omit=dev --audit-level=moderate` |
| 35 | `contracts.audit.full` | `contracts` | `npm audit --audit-level=moderate` |
| 36 | `bytecode.compare` | repository | replay the exact preserved configuration-only C2/explicit-Shanghai manifest comparison below; require equality, 43 compiler contracts, 27 user artifacts and exact output SHA-256 `9D75271BB9A9E87D410D98DC9EAE4F0ADAA8597B7D55E4A72324EC2BEA78AF62` |
| 37 | `compose.render.raw` | repository | `docker compose --env-file config/production.env.example -f docker-compose.prod.yml config --quiet` |
| 38 | `repository.status.ignored-aware` | repository, after cleanup | `git status --porcelain=v1 --untracked-files=all --ignored=matching`; require zero lines |

The exact `web.sharp-smoke` checked argument is the following single Node
script. It uses a solid-red 2x3 RGBA fixture and rejects the previously
observed 92-byte white-fixture result:

```javascript
(async()=>{const sharp=require('./apps/web/node_modules/sharp');const pixel=[255,0,0,255];const input=Buffer.from(Array(6).fill(pixel).flat());const png=await sharp(input,{raw:{width:2,height:3,channels:4}}).png().toBuffer();const meta=await sharp(png).metadata();const versions=sharp.versions;if(png.length!==95||meta.format!=='png'||meta.width!==2||meta.height!==3||versions.sharp!=='0.35.3'||versions.vips!=='8.18.3')throw new Error(JSON.stringify({bytes:png.length,meta,versions}));console.log(`SHARP_SMOKE_PASS|fixture=solid-red-rgba|bytes=${png.length}|format=${meta.format}|width=${meta.width}|height=${meta.height}|sharp=${versions.sharp}|vips=${versions.vips}`)})().catch(e=>{console.error(e);process.exit(1)});
```

For `bytecode.compare`, the runner must expose mandatory, non-skippable
parameters for both exact preserved manifests and the immutable commit/tree/tool
bindings. It must verify the input hashes before invocation and print the fully
expanded argument vector with paths and hashes. A missing or mismatched input
is a failure, not a reason to omit the label:

- baseline manifest:
  `C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721\bytecode-baseline-c2.json`;
  SHA-256
  `7C5CED696A38C2DEB5AFEFB70A61B8F3D0E87AD341905A31137ACFDC522359E0`;
- preserved candidate manifest:
  `C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721\bytecode-explicit-shanghai.json`;
  SHA-256
  `CF45D49FE9CCD30A48ACAB71229AAC05A4E09AA9F36600BD82B5CF26D59CC186`.

The exact preserved comparison argument vector is:

```text
node contracts/scripts/compare-bytecode-manifests.mjs
  --baseline {mandatory_BaselineManifest_parameter}
  --candidate {mandatory_CandidateManifest_parameter}
  --capture-script contracts/scripts/capture-bytecode-manifest.mjs
  --git-executable {resolved_git_executable}
  --git-repo {resolved_repository_root}
  --repo-contracts-root contracts
  --baseline-commit a88658ad82f3d22aaf26e10b9eab6389084e6dd3
  --baseline-tree 13492dba6a709db7d093052824c5ddb3ee8c58e9
  --candidate-commit 2a55a5fbf51a2018a650122eff313f3ce4e63dff
  --candidate-tree 6b9f6672001b0f9d14d0dcb19756b206db2974cd
  --tool-commit 2a55a5fbf51a2018a650122eff313f3ce4e63dff
  --tool-tree 6b9f6672001b0f9d14d0dcb19756b206db2974cd
  --tool-path contracts/scripts/capture-bytecode-manifest.mjs
  --out {fresh_comparison_output}
```

The fresh comparison output must have SHA-256
`9D75271BB9A9E87D410D98DC9EAE4F0ADAA8597B7D55E4A72324EC2BEA78AF62`
and report equality for 43 compiler contracts and 27 user artifacts. This is a
replay of preserved configuration/toolchain evidence only. It is not a capture
of I5, R5 or V5 contract bytecode and does not assert that any later Solidity
source is byte-equal to C2. Current repository source is covered separately by
the checked compile, typecheck, full test and targeted 16/36/5 test labels.

The web fixture environment is absent or restored until immediately before
label 18. Labels 18-25 run with only
`NEXT_PUBLIC_API_URL=https://api.blockxone.example` and
`SERVER_ACTION_ALLOWED_ORIGINS=app.blockxone.example` added to the inherited
process environment. Both variables and the prior location are restored after
label 25 even on failure. Compile label 28 precedes typecheck label 29 because
generated types are required.

Expected nonzero discovery/evidence floors are: Compose policy 4 services and
29 negative fixtures; Go 357 passes across 12 tested packages with zero
failure/skip; web preflight 4, config 5, runner 21, both unit entries 92, build
45 pages; contract preflight 10, compile 27 Solidity files under solc
0.8.20/Shanghai, full tests 59 and targeted 16/36/5. Audits must report zero
vulnerabilities at their configured thresholds; gosec must report zero issues;
govulncheck must report zero reachable vulnerabilities. Drift in incidental
package/path/commit counts is recorded and reviewed, not silently hardcoded as
success.

The generated-output inventory is exactly 20 parent-safe entries: repository
`node_modules` and `.npm-cache`; web `node_modules`, `.next`, `coverage`,
`.vite`, `.vitest`, `.npm-cache`, `out`, `build`, `dist`,
`tsconfig.tsbuildinfo`, `next-env.d.ts`; contracts `node_modules`, `artifacts`,
`.hardhat-cache`, `cache`, `typechain-types`, `.npm-cache`, `coverage`.
`apps/web/node_modules/.vite` is not a separate entry because the validated
parent move preserves it.

## Mandatory runner tests

`.planning/scripts/test-phase0-matrix-runner.ps1` must fail closed and be safe
to run without package installation, network access or real-repository
mutation. It may use the already resolved pinned Node binary and GSD tool in
fresh disposable Git fixtures.

At minimum it must:

- parse the runner with the PowerShell AST and fail on any parse error;
- reject missing, nonexistent, non-leaf or SHA-mismatched
  `-NodeExecutable`/`-NpmCli`/`-GoExecutable` inputs, wrong tool versions and
  any ambient-PATH fallback;
- prove every logical npm label invokes the pinned Node executable with the
  pinned npm CLI as its first argument, and every Go label invokes the pinned
  Go executable;
- prove the only call-operator native launch in the runner is inside
  `Invoke-NativeChecked`;
- reject bare external commands and alternate launch paths outside
  `Invoke-NativeChecked`, including `Start-Process`, `Invoke-Expression`,
  `System.Diagnostics.Process`, `cmd`, Windows PowerShell, WSL, dot-sourcing,
  dynamically constructed command names and nested shell launchers;
- prove every production native step is represented by the exact expected
  labeled invocation, with no duplicate, missing or extra mandatory label;
- run the summary-present and no-summary planning-validator mutation suites
  against disposable 12-plan fixtures, including exact `partial`/8,
  `planned`/0, 89 unchecked requirements and ignored-aware residue;
- run an isolated success process and require exit `0`;
- run an isolated failure process with exit `7` and require the wrapper to
  surface label plus exit `7`;
- mutation-test the historical bug by inserting a later exit-`0` process
  before the wrapper reads `$LASTEXITCODE`, and require the self-test/static
  contract to reject that mutated runner;
- mutation-test a direct unwrapped native invocation and require rejection;
- test missing executable resolution;
- test that a primary failure remains primary when cleanup succeeds;
- test successful matrix plus cleanup failure and require a nonzero result;
- test primary exit 7 plus a first cleanup failure plus a later cleanup sentinel:
  require that the sentinel was attempted, the primary label/exit 7 remains
  primary, every cleanup error is aggregated and the result is nonzero;
- test exact environment and location restoration, including both previously
  absent and previously valued `GOTOOLCHAIN` states;
- create terminal-reparse and ancestor-junction fixtures and require rejection
  before any source or target is moved;
- pre-create the exact destination and require failure with the source
  byte-identical and not nested, overwritten or moved;
- inject destination creation after validation and immediately before the
  atomic move; require failure with no overwrite, nesting or source movement;
- reject a reparse-point temporary/quarantine/destination ancestor fixture;
- prove a parent output move preserves its nested `.vite` content without a
  separate nested-source move;
- use only fresh absent OS-temporary fixture roots;
- never recurse over, delete, clean, reset or amend the real repository; and
- leave the real repository files byte-identical and both ordinary and
  ignored-aware status empty.

## Tasks

<task type="auto" category="evidence" id="00-12-01">
  <name>Task 1: Freeze the failed R4 attempt and accept Plan 00-12 as P11</name>
  <files>
    .planning/phases/00-planning-truth-and-containment/00-12-PLAN.md;
    external SECURITY-REVIEW-FAILURE-RECEIPT-v4.md;
    external GEN3-P11-NATIVE-EXIT-PLAN-ACCEPTANCE.md
  </files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-11-PLAN.md,
    .planning/TEST-CONTRACT.md,
    .planning/scripts/validate-planning.ps1,
    TASK6-I4-BUILDER-RECEIPT.md,
    exact-I4 mutation logs,
    every Generation 3 acceptance/review/verification/failure receipt
  </read_first>
  <action>
    Keep the attempted R4 reviewer read-only. The failure receipt must
    time-bind the reviewer's initial clean-I4 freeze and separately disclose
    the later author-owned untracked Plan 00-12 file; that expected plan draft
    is not an R4 cleanliness acceptance. Require the reviewer to finish the
    bounded audit and write
    SECURITY-REVIEW-FAILURE-RECEIPT-v4.md. The receipt must bind exact I4,
    reproduce exit 7 followed by exit 0 yielding a false overall success,
    enumerate every unguarded native command class, classify
    TEST_CONTRACT_NATIVE_EXIT_MASKING as FIX-HIGH/hard stop, and state that no
    R4 commit exists. It must also record the ancestor-junction containment
    finding, primary-error replacement finding and missing agent-role
    provenance finding with their typed root causes. It must correct the
    overbroad I4 receipt wording by distinguishing tracked working-tree
    immutability from the six disclosed, recoverable `.git/objects` moves.

    The same receipt must bind the immutable I4 builder receipt hash, name its
    four stale printed paths, name the four correct retained paths, hash the
    exact-I4 and no-summary out/error logs, and distinguish retained underlying
    mutation evidence from the stale location statements.
    Keep the fixed-hash v4 failure receipt unchanged: it carries the attempted
    R4 reviewer/subreview task paths and transparently diagnoses the missing
    earlier-role provenance; it does not retroactively acquire Plan 00-11
    author/checker or I4-builder paths. Bind the controller-known canonical
    task paths for the Plan 00-11 author, Plan 00-11 checker, I4 builder and
    attempted R4 reviewer in the new
    GEN3-P11-NATIVE-EXIT-PLAN-ACCEPTANCE.md, while explicitly stating that
    transcript-derived task-path provenance is not legal-person identity or
    professional independence.

    Use a plan checker distinct from this plan's author, I4 builder, failed R4
    reviewer, future I5 builder, R5 reviewer, V5 verifier and C5 owner. The
    checker must review the complete plan, verify that the path and native-exit
    defects are covered, confirm the runner cannot skip any existing mandatory
    matrix step, confirm source and destination reparse/collision containment,
    confirm the nine-path I5 allowlist, and reject any production or
    professional authority expansion.

    Before committing, require ignored-aware status to contain exactly
    `?? .planning/phases/00-planning-truth-and-containment/00-12-PLAN.md`.
    Commit only Plan 00-12 as P11 with exact I4 as parent.

    Write GEN3-P11-NATIVE-EXIT-PLAN-ACCEPTANCE.md with exact I4/P11 commits,
    trees and parent, plan SHA-256, checker identity and verdict, v4 failure
    receipt hash, old builder receipt hash, corrected path/log evidence, exact
    canonical agent task paths and the future role-separation contract,
    one-branch/zero-remote proof, ordinary and ignored-aware clean status,
    silent strict/full-unreachable fsck and physical-equals-reachable counts.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; $expected='ff24f9d79f4a02e3cac054283140a2ed3226d5c5'; $parent=(git rev-parse 'HEAD^').Trim(); if($LASTEXITCODE -ne 0 -or $parent -cne $expected){exit 1}; $paths=@(git diff-tree --no-commit-id --name-only -r HEAD); if($LASTEXITCODE -ne 0 -or $paths.Count -ne 1 -or $paths[0] -cne '.planning/phases/00-planning-truth-and-containment/00-12-PLAN.md'){exit 1}; $status=@(git status --porcelain=v1 --untracked-files=all --ignored=matching); if($LASTEXITCODE -ne 0 -or $status.Count -ne 0){exit 1}; $failure='C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721\SECURITY-REVIEW-FAILURE-RECEIPT-v4.md'; if((Get-FileHash -LiteralPath $failure -Algorithm SHA256).Hash -cne '0D9B3DF8697646FBB4747BD12B3521DA39E898C286593809CF9C3492176DD7B5'){exit 1}"</automated>
    `parent(P11)=I4`; `I4..P11` is only `00-12-PLAN.md`; both external receipts
    exist and hash; ignored-aware status is empty; zero remotes; strict and
    full-unreachable fsck are silent.
  </verify>
  <acceptance_criteria>
    - The v4 failure receipt preserves exact I4 and states that no R4 commit was
      created.
    - Exit masking and stale evidence paths are reproduced and typed.
    - Ancestor-junction containment, error preservation and role provenance
      omissions are reproduced and typed.
    - The old tracked-path/object-store scope overstatement is explicitly
      corrected without hiding or reclassifying the six preserved object moves.
    - The correct retained paths and matrix-log hashes are recorded.
    - P11 is plan-only, independently accepted and clean under ignored-aware
      status.
    - C2 and every old commit and receipt remain byte-for-byte unchanged.
  </acceptance_criteria>
  <done>P11 truthfully freezes the stopped R4 gate and accepted correction.</done>
  <commit>docs(phase-00): plan fail-closed native matrix execution</commit>
</task>

<task type="auto" category="evidence" id="00-12-02">
  <name>Task 2: Implement the checked runner and freeze I5</name>
  <files>
    .planning/TEST-CONTRACT.md;
    .planning/scripts/invoke-phase0-matrix.ps1;
    .planning/scripts/test-phase0-matrix-runner.ps1;
    .planning/scripts/validate-planning.ps1;
    .planning/BLOCKERS.md;
    .planning/EVIDENCE-REGISTER.md;
    .planning/STATE.md;
    .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md;
    .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md;
    external TASK6-I5-BUILDER-RECEIPT.md
  </files>
  <read_first>
    Plan 00-12 and its acceptance receipt,
    SECURITY-REVIEW-FAILURE-RECEIPT-v4.md,
    all nine repository paths listed above,
    current CI workflows and package preflight scripts,
    all prior builder/review/verification receipts
  </read_first>
  <action>
    Use a builder who did not author or check Plan 00-12 and who will not act as
    R5 reviewer, V5 verifier or C5 admission owner.

    Add the runner and its isolated tests exactly as specified above. Replace
    TEST-CONTRACT's copy/paste native sequence with exactly one invocation of
    the runner and no second outer native command. The runner invokes the
    isolated runner-test script as checked label `runner.tests`. Keep the full
    frozen command inventory, scopes, arguments and order. Do not weaken audit
    levels, test discovery, containment, ignored-aware cleanup, version pins or
    NO-GO language.

    Replace the nested-first quarantine list with a containment-safe inventory.
    Before any move, validate the component root and every existing lexical
    component through the terminal source as non-reparse. Do not call
    Resolve-Path through an unvalidated ancestor. Do not separately move
    `node_modules/.vite` when the validated parent `node_modules` move already
    preserves it. Validate the temporary/quarantine/destination ancestors,
    require same-volume moves, and use file/directory APIs that fail on an
    existing destination rather than Move-Item's directory-nesting behavior.
    Continue bounded cleanup after an individual cleanup error, collect every
    cleanup error, and preserve the primary matrix error.

    Update validate-planning.ps1 to accept exactly Plans 00-01 through 00-12.
    With no summary, require GSD `planned`/0. With the one permitted summary,
    require `partial`/8. Require STATE total_plans 12, completed_plans 0,
    Phase 0 in_progress, 89 unchecked requirements and production completion
    zero. BuilderStaged mode must accept exactly the nine Task 2 paths,
    including the two new index-added scripts, and reject any missing, extra,
    unstaged, worktree-only, ordinary-untracked or ignored path.
    The only accepted status codes are exactly `A ` for
    `.planning/scripts/invoke-phase0-matrix.ps1` and
    `.planning/scripts/test-phase0-matrix-runner.ps1`, and exactly `M ` for
    each of the other seven Task 2 paths. Explicitly reject `AM`, `MM`,
    deletions, renames, copies, unstaged changes, `??`, `!!`, missing paths and
    extra paths.

    Update BLOCKERS, EVIDENCE-REGISTER, STATE, 00-04-SUMMARY and 00-EVIDENCE
    with exact current truth: I4's aggregate matrix PASS is not accepted
    because mandatory native exits were maskable; its individual outputs are
    narrow historical evidence only; the attempted R4 produced only
    SECURITY-REVIEW-FAILURE-RECEIPT-v4.md; no R4, V4 or C4 exists; and the six
    recoverably moved `.git/objects` files were repository-metadata mutations,
    not tracked working-tree edits. These statements must also bind the v4
    failure receipt hash, P11 plan hash/identity, pairwise-distinct role
    contract, typed root causes and retained NO-GO.

    Extend validator mutations for plan 12/count, partial/8, exact nine-path
    staging, new-script status classes, runner-policy summary statements and
    native-exit masking. Mutation-test every exact current-truth statement in
    the five evidence/state files above, including false aggregate PASS, narrow
    historical evidence, absent R4/V4/C4 and Git-object metadata wording.
    Preserve the no-summary and summary-bearing states and restore
    byte-identical originals after every disposable mutation.

    Stage exactly the nine Task 2 paths. Run the isolated runner tests and
    staged validator mutations. Commit exactly those nine paths as I5 with P11
    as parent.

    At exact clean I5, invoke the complete matrix only through the new runner
    with pinned Node 22.23.1/npm 10.9.8 and Go 1.26.5. Re-run the runner tests,
    validator mutations, the preserved configuration-only bytecode comparator
    replay, and targeted identity, issuance and web containment tests
    independently of the runner result. Quarantine all known
    generated output without overwrite, then require ordinary and ignored-aware
    status both empty.

    Write TASK6-I5-BUILDER-RECEIPT.md bound to exact I5, every input receipt
    hash, the runner/test-script hashes, all tool identities, command labels,
    self-test/mutation counts, full matrix results, corrected evidence paths
    and final topology. Record the I5 builder's canonical agent task path and
    the explicit role-separation matrix.
  </action>
  <verify>
    <automated>pwsh -NoProfile -File .\.planning\scripts\invoke-phase0-matrix.ps1 -NodeExecutable "C:\Users\danie\AppData\Local\npm-cache\_npx\d295cebdb7c54afe\node_modules\node\bin\node.exe" -NpmCli "C:\Users\danie\AppData\Local\npm-cache\_npx\d8f2e63c6145eb9d\node_modules\npm\bin\npm-cli.js" -GoExecutable "C:\Users\danie\go\pkg\mod\golang.org\toolchain@v0.0.1-go1.26.5.windows-amd64\bin\go.exe" -BaselineManifest "C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721\bytecode-baseline-c2.json" -CandidateManifest "C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721\bytecode-explicit-shanghai.json" -BaselineCommit a88658ad82f3d22aaf26e10b9eab6389084e6dd3 -BaselineTree 13492dba6a709db7d093052824c5ddb3ee8c58e9 -CandidateCommit 2a55a5fbf51a2018a650122eff313f3ce4e63dff -CandidateTree 6b9f6672001b0f9d14d0dcb19756b206db2974cd -ToolCommit 2a55a5fbf51a2018a650122eff313f3ce4e63dff -ToolTree 6b9f6672001b0f9d14d0dcb19756b206db2974cd -ToolPath "contracts/scripts/capture-bytecode-manifest.mjs"</automated>
    `parent(I5)=P11`; `P11..I5` is exactly the nine Task 2 paths; product source
    equals I4; runner and isolated tests pass; every native step has an
    immediate checked result; forced exit-7 and masking mutations fail closed;
    plan count 12, partial/8, production completion zero; exact-I5 full matrix
    passes; ignored-aware status is empty; zero remotes.
  </verify>
  <acceptance_criteria>
    - No mandatory native command is launched directly outside the checked
      wrapper.
    - A failing native command cannot be converted into success by a later
      successful command.
    - Primary and cleanup failures remain separately diagnosable and nonzero.
    - Ancestor and terminal reparse fixtures are rejected before any move.
    - A pre-existing destination is rejected atomically with the source
      byte-identical and unmoved.
    - TEST-CONTRACT invokes the repository-owned runner and retains every
      previous mandatory gate.
    - I5 changes no product, ROADMAP, REQUIREMENTS, CI, Compose, package,
      contract or dependency-lock path.
    - New evidence corrects the stale I4 receipt paths without altering the old
      receipt.
  </acceptance_criteria>
  <done>I5 is a truthful nine-file fail-closed evidence correction over P11.</done>
  <commit>docs(phase-00): enforce fail-closed native matrix execution</commit>
</task>

<task type="auto" category="security" id="00-12-03">
  <name>Task 3: Independently review exact I5 and create R5</name>
  <files>
    .planning/phases/00-planning-truth-and-containment/00-SECURITY-REVIEW.md;
    external SECURITY-REVIEW-RECEIPT-v5.md
  </files>
  <read_first>
    Plan 00-12,
    SECURITY-REVIEW-FAILURE-RECEIPT-v4.md,
    GEN3-P11-NATIVE-EXIT-PLAN-ACCEPTANCE.md,
    TASK6-I5-BUILDER-RECEIPT.md,
    all nine I5 paths and every preserved Generation 3 receipt
  </read_first>
  <action>
    Use an adversarial reviewer distinct from the Plan 00-12 author/checker,
    I5 builder, V5 verifier and C5 owner. Freeze exact clean I5. Re-run the
    isolated runner tests, all native-exit and path-provenance mutations, the
    complete security-relevant matrix, dependency audits, the preserved
    configuration-only bytecode comparator replay and targeted
    identity/issuance/web-containment tests. Do not characterize that replay
    as current-I5 bytecode equality.

    Independently inject a native exit 7 followed by a native exit 0 into a
    disposable runner fixture and require the fixture to fail. Verify every
    mandatory production label maps to exactly one checked invocation and that
    no runner mode skips a real-matrix gate. Reproduce the ignored next-env
    negative probe and require ordinary and ignored-aware status empty after
    bounded quarantine.

    Independently create a safe disposable ancestor-junction fixture and
    terminal-reparse fixture and require rejection before any source or
    destination mutation. Pre-create an exact destination and require the
    source to remain byte-identical and unmoved. Verify that one cleanup failure
    does not suppress the primary failure or prevent remaining bounded cleanup
    attempts.

    Write SECURITY-REVIEW-RECEIPT-v5.md bound to I5. Replace only the current
    internal SECURITY-REVIEW file with a transparent R5 report that identifies
    the V3 ignored-output failure, the attempted R4 native-exit failure, the
    stale I4 receipt paths and their corrected provenance, preserves every
    retained NO-GO boundary, and states that its verdict binds I5 rather than
    its own future commit. Commit only that report as R5.
    It must also explicitly supersede the current historical reports' broad
    false-success/generated-cleanliness wording and the I4 receipt's overbroad
    tracked-path/object-store wording while preserving those old files in prior
    commits and receipts.
    The receipt must name the reviewer's canonical agent task path and compare
    it to every earlier and future role represented at that point.
  </action>
  <verify>
    <automated>pwsh -NoProfile -File .\.planning\scripts\invoke-phase0-matrix.ps1 -NodeExecutable "C:\Users\danie\AppData\Local\npm-cache\_npx\d295cebdb7c54afe\node_modules\node\bin\node.exe" -NpmCli "C:\Users\danie\AppData\Local\npm-cache\_npx\d8f2e63c6145eb9d\node_modules\npm\bin\npm-cli.js" -GoExecutable "C:\Users\danie\go\pkg\mod\golang.org\toolchain@v0.0.1-go1.26.5.windows-amd64\bin\go.exe" -BaselineManifest "C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721\bytecode-baseline-c2.json" -CandidateManifest "C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721\bytecode-explicit-shanghai.json" -BaselineCommit a88658ad82f3d22aaf26e10b9eab6389084e6dd3 -BaselineTree 13492dba6a709db7d093052824c5ddb3ee8c58e9 -CandidateCommit 2a55a5fbf51a2018a650122eff313f3ce4e63dff -CandidateTree 6b9f6672001b0f9d14d0dcb19756b206db2974cd -ToolCommit 2a55a5fbf51a2018a650122eff313f3ce4e63dff -ToolTree 6b9f6672001b0f9d14d0dcb19756b206db2974cd -ToolPath "contracts/scripts/capture-bytecode-manifest.mjs"</automated>
    Full exact-I5 review evidence; zero unresolved critical/high, dependency,
    evidence-truth or false-success findings; `parent(R5)=I5`; `I5..R5` is only
    `00-SECURITY-REVIEW.md`; final ordinary/ignored-aware status empty.
  </verify>
  <acceptance_criteria>
    - Reviewer identity is distinct from author/checker, builder, verifier and
      admission owner.
    - Exit masking and stale path defects are independently reproduced as
      fixed, not merely asserted.
    - Ancestor-junction containment, error aggregation and task-path role
      provenance are independently reproduced as fixed.
    - R5 changes one repository file and does not claim Phase 0 or production
      readiness.
  </acceptance_criteria>
  <done>R5 independently accepts exact I5 for verification, not production.</done>
  <commit>docs(phase-00): review fail-closed native matrix execution</commit>
</task>

<task type="auto" category="verify" id="00-12-04">
  <name>Task 4: Independently verify exact R5 and create V5</name>
  <files>
    .planning/phases/00-planning-truth-and-containment/00-VERIFICATION.md;
    external VERIFICATION-RECEIPT-v5.md
  </files>
  <read_first>
    Plan 00-12,
    all nine I5 paths,
    SECURITY-REVIEW-FAILURE-RECEIPT-v4.md,
    TASK6-I5-BUILDER-RECEIPT.md,
    SECURITY-REVIEW-RECEIPT-v5.md,
    every preserved Generation 3 receipt
  </read_first>
  <action>
    Use a verifier distinct from all prior roles. At exact clean R5, rerun the
    complete runner, runner tests, validator and mutations, acceptance matrix,
    preserved configuration-only bytecode comparator replay, receipt-hash
    checks, allowlist/immutable comparisons and exact ignored-file negative
    probe. Do not infer current-R5 bytecode equality. Independently prove that a native
    exit-7 result cannot be masked by a later exit-0 result.

    Record every criterion as exact pass, narrow supersession, retained NO-GO
    or pending C5 admission; any failure blocks the commit.
    Record the verifier's canonical agent task path and the complete
    role-separation matrix available at verification time.

    Write VERIFICATION-RECEIPT-v5.md bound to R5. Replace only the current
    internal VERIFICATION file with a transparent V5 report that binds its
    verdict to R5 and keeps Phase 0 in_progress/NO-GO. Commit only that report
    as V5, then prove its parent and one-file diff.
  </action>
  <verify>
    <automated>pwsh -NoProfile -File .\.planning\scripts\invoke-phase0-matrix.ps1 -NodeExecutable "C:\Users\danie\AppData\Local\npm-cache\_npx\d295cebdb7c54afe\node_modules\node\bin\node.exe" -NpmCli "C:\Users\danie\AppData\Local\npm-cache\_npx\d8f2e63c6145eb9d\node_modules\npm\bin\npm-cli.js" -GoExecutable "C:\Users\danie\go\pkg\mod\golang.org\toolchain@v0.0.1-go1.26.5.windows-amd64\bin\go.exe" -BaselineManifest "C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721\bytecode-baseline-c2.json" -CandidateManifest "C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721\bytecode-explicit-shanghai.json" -BaselineCommit a88658ad82f3d22aaf26e10b9eab6389084e6dd3 -BaselineTree 13492dba6a709db7d093052824c5ddb3ee8c58e9 -CandidateCommit 2a55a5fbf51a2018a650122eff313f3ce4e63dff -CandidateTree 6b9f6672001b0f9d14d0dcb19756b206db2974cd -ToolCommit 2a55a5fbf51a2018a650122eff313f3ce4e63dff -ToolTree 6b9f6672001b0f9d14d0dcb19756b206db2974cd -ToolPath "contracts/scripts/capture-bytecode-manifest.mjs"</automated>
    Exact-R5 full matrix and typed criteria; receipt hashes; ignored-aware
    cleanliness before the report edit and after V5; `parent(V5)=R5`;
    `R5..V5` is only `00-VERIFICATION.md`; zero remotes.
  </verify>
  <acceptance_criteria>
    - Every mandatory exact-R5 gate passes with nonzero test discovery.
    - No stale native-exit or evidence-path claim remains in current reports or
      new receipts.
    - V5 is a one-file evidence-only child of R5.
    - Exact-V5 admission, Gitleaks and final topology remain explicitly pending.
  </acceptance_criteria>
  <done>V5 independently verifies exact R5 and is ready for final admission.</done>
  <commit>docs(phase-00): verify fail-closed native matrix execution</commit>
</task>

<task type="auto" category="admission" id="00-12-05">
  <name>Task 5: Admit exact V5 as local-only C5</name>
  <files>external CANDIDATE-MANIFEST-v5.md only</files>
  <read_first>
    Plans 00-04 through 00-12,
    all current Phase 0 planning/evidence/report files,
    all old and v4/v5 external receipts,
    .planning/TEST-CONTRACT.md,
    invoke-phase0-matrix.ps1 and its tests
  </read_first>
  <action>
    Use a final-admission owner distinct from the Plan 00-12 author/checker,
    builder, reviewer and verifier.
    Freeze exact V5 and require ordinary and ignored-aware status empty before
    running anything. Run the complete matrix through the checked runner, its
    isolated/mutation tests, exact identity/issuance/containment tests, the
    preserved configuration-only bytecode comparator replay and runner-failure
    injections at V5. Do not infer current-V5 bytecode equality. Quarantine every known
    generated output without overwrite and require both status forms empty.

    Prove the I4->P11->I5->R5->V5 parent/path contracts, the full Plan 00-04
    through 00-12 union, product-source equality to I4 and I3, immutable
    ROADMAP and REQUIREMENTS, all receipt hashes, silent strict/full-unreachable
    fsck, exactly one branch/worktree, zero remotes/tags/replace/grafts/
    alternates/shared/shallow/partial/promisor state and
    physical-equals-reachable objects.

    Run unchanged redacted Gitleaks 8.30.1 git/tree wrappers and the unchanged
    repository-artifact validator. Re-snapshot C2 read-only and require exact
    commit/tree/config/manifest/ordinary-status/ignored-inventory equality to
    the P10 receipt. Do not call C2 filesystem-clean and do not move, clean or
    quarantine anything from C2.

    Only after every check passes, create external
    CANDIDATE-MANIFEST-v5.md and define `C5=V5`. Record commits/trees, plan and
    receipt hashes, runner/test hashes, command labels, forced-failure proofs,
    scanner counts, tool versions, quarantine locations, matrix results and
    typed verdicts. State local-only, Phase 0 in_progress/NO-GO and every
    human/professional/hosted/production gate. Do not edit, stage or commit any
    repository file.
    Record the final-admission owner's canonical agent task path and the
    completed task-path role-separation matrix, without presenting task-path
    separation as professional independence.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; pwsh -NoProfile -File .\.planning\scripts\invoke-phase0-matrix.ps1 -NodeExecutable 'C:\Users\danie\AppData\Local\npm-cache\_npx\d295cebdb7c54afe\node_modules\node\bin\node.exe' -NpmCli 'C:\Users\danie\AppData\Local\npm-cache\_npx\d8f2e63c6145eb9d\node_modules\npm\bin\npm-cli.js' -GoExecutable 'C:\Users\danie\go\pkg\mod\golang.org\toolchain@v0.0.1-go1.26.5.windows-amd64\bin\go.exe' -BaselineManifest 'C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721\bytecode-baseline-c2.json' -CandidateManifest 'C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721\bytecode-explicit-shanghai.json' -BaselineCommit a88658ad82f3d22aaf26e10b9eab6389084e6dd3 -BaselineTree 13492dba6a709db7d093052824c5ddb3ee8c58e9 -CandidateCommit 2a55a5fbf51a2018a650122eff313f3ce4e63dff -CandidateTree 6b9f6672001b0f9d14d0dcb19756b206db2974cd -ToolCommit 2a55a5fbf51a2018a650122eff313f3ce4e63dff -ToolTree 6b9f6672001b0f9d14d0dcb19756b206db2974cd -ToolPath 'contracts/scripts/capture-bytecode-manifest.mjs'; $runnerExit=$LASTEXITCODE; if($runnerExit -ne 0){exit $runnerExit}; $manifest='C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721\CANDIDATE-MANIFEST-v5.md'; if(-not (Test-Path -LiteralPath $manifest -PathType Leaf)){exit 1}; $status=@(git status --porcelain=v1 --untracked-files=all --ignored=matching); if($LASTEXITCODE -ne 0 -or $status.Count -ne 0){exit 1}"</automated>
    Exact-V5 full matrix; exit masking fails closed; ignored-aware clean
    boundary before and after; zero safe scanner findings; exact lineage,
    allowlist, immutable, topology and object proofs; exact C2 read-only
    equality; external manifest hash; no Git change.
  </verify>
  <acceptance_criteria>
    - Exact V5 passes every local test and admission command.
    - Native exit 7 followed by exit 0 remains an observable admission failure.
    - CANDIDATE-MANIFEST-v5.md binds exact C5=V5 without a Git change.
    - The manifest distinguishes local technical evidence from unresolved
      hosted, legal, financial, security, incident and production gates.
    - No remote or production-side action occurs.
  </acceptance_criteria>
  <done>C5 is the exact local-only V5 candidate; Phase 0 remains NO-GO.</done>
</task>

## Plan verification checklist

- [ ] The attempted R4 review is frozen externally with no R4 commit.
- [ ] The four stale I4 receipt paths and four correct retained paths are bound.
- [ ] P11 is an independently accepted plan-only child of I4.
- [ ] I5 is the exact nine-file corrective evidence child of P11.
- [ ] Plan count is 12, summary state is partial/8 and production completion is 0.
- [ ] Every native process is routed through one immediately checked wrapper.
- [ ] Exit 7 followed by exit 0 fails in disposable runner mutations.
- [ ] Primary and cleanup failure diagnostics are both retained.
- [ ] Ancestor and terminal reparse fixtures fail before any quarantine move.
- [ ] Pre-existing destinations fail without nesting, overwrite or source move.
- [ ] New receipts contain canonical task-path role provenance and its limits.
- [ ] Exact I5 and R5 pass their role-specific full matrices.
- [ ] R5 and V5 retain exact one-file report ancestry.
- [ ] Exact V5 passes the full matrix and repository admission.
- [ ] CANDIDATE-MANIFEST-v5.md binds C5=V5 without a Git change.
- [ ] Phase 0 and all requirements remain incomplete; release remains NO-GO.
- [ ] No remote, push, deployment, RPC, wallet, key, provider, funds, real
      asset, professional approval or irreversible action occurs.

<success_criteria>

- A nonzero mandatory native command can never be overwritten into success by
  a later native command.
- The runner's checked command inventory is executable and mutation-tested.
- Generated-output quarantine cannot traverse an unvalidated ancestor reparse
  point and continues bounded cleanup while preserving all failure diagnostics.
- The immutable I4 receipt remains preserved while its four stale path
  statements are transparently corrected in all new evidence.
- Every old commit and receipt remains hash-verifiable.
- The corrected candidate has complete independent local evidence ancestry.
- Financial and blockchain release gates remain fail closed: no mock/provider
  path, payment or mint finality, legal register, reconciliation, deployment,
  key governance, external audit or professional sign-off is inferred complete.

</success_criteria>

<failure_and_rollback>

- Any false claim, direct unwrapped native process, masked exit, extra
  repository path, missing matrix gate, ignored residue, completed production
  state, mutation escape, stale evidence, scanner finding, role conflict or
  ambiguous authority is a hard stop.
- This is remediation cycle one for
  `TEST_CONTRACT_NATIVE_EXIT_MASKING`,
  `EVIDENCE_PROVENANCE_PATH_TRANSCRIPTION_ERROR`,
  `GENERATED_OUTPUT_ANCESTOR_REPARSE_ESCAPE` and
  `AGENT_ROLE_PROVENANCE_OMISSION`, plus
  `EVIDENCE_SCOPE_WORDING_OVERSTATEMENT`. A repeated failure of any same root
  cause exhausts the two-cycle ceiling and requires fresh user review and a
  newly accepted plan.
- Never amend or overwrite C2, I3, R3, V3, P10, I4, their old receipts or an
  admitted C5.
- Quarantine only exact resolved paths beneath a fresh resolved OS-temporary
  root, require absent destinations and preserve hashes. Never use a broad,
  wildcard or repository-recursive delete.

</failure_and_rollback>

<output>

Repository outputs are limited to Plan 00-12, the nine-file I5 correction and
the one-file R5/V5 reports. External outputs use deterministic v4 failure and
v5 success filenames under the existing Generation 3 evidence directory.

</output>
