---
phase: 00-planning-truth-and-containment
plan: 08
type: execute
name: generation 3 July 23 PostCSS advisory supersession
status: proposed
gap_closure: true
wave: 1
depends_on: []
autonomous: true
risk_class: critical
requirements: [BASE-03, BASE-04, BASE-06, BASE-07]
user_setup: []
execution_mode: controller-dispatched-serial-tasks
requirements_completion: forbidden
files_modified:
  - .planning/phases/00-planning-truth-and-containment/00-08-PLAN.md
  - apps/web/package.json
  - apps/web/package-lock.json
  - .planning/scripts/validate-planning.ps1
  - .planning/EVIDENCE-REGISTER.md
  - .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md
  - .planning/BLOCKERS.md
  - .planning/STATE.md
  - .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md
must_haves:
  truths:
    - "The July 2026 Next.js, Sharp and PostCSS findings are all absent before I3."
    - "The dependency change remains exactly two web package files; application source is unchanged."
    - "Next 15.5.21, Sharp 0.35.3/libvips 8.18.3 and PostCSS 8.5.22 are proven by resolution, native smoke, audits and build."
    - "The current phase contains exactly eight plans while authoritative Phase 0 progress remains zero and NO-GO."
    - "The complete matrix passes at exact W6 before I3 and again at exact I3."
  artifacts:
    - "apps/web/package.json"
    - "apps/web/package-lock.json"
    - ".planning/scripts/validate-planning.ps1"
    - ".planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md"
  key_links:
    - "P7 is a plan-only child of P6; W6 is a two-file web-dependency child of P7."
    - "I3 is a six-file evidence child of W6; R3 and V3 retain one-file report ancestry."
    - "Plans 00-04 through 00-08 form the Generation 3 union allowlist."
---

# Plan 00-08: Generation 3 July 23 PostCSS advisory supersession

## Objective

Supersede the now-stale Plan 00-07 web candidate with a combined exact
Next.js, Sharp and PostCSS resolution that passes the live advisory data,
without changing application source or weakening any gate.

## Approved local authority

The user approved continued execution of the controlled local delivery loop on
2026-07-23. That authority covers local planning, exact dependency resolution,
tests, evidence, independent review, independent verification and local
commits.

It does not authorize a remote, push, deployment, RPC, provider, wallet, key,
funds, real assets, production network, production configuration, professional
approval or irreversible action.

## Trigger and confirmed root cause

Plan 00-07 was independently accepted as P6
`5a489930296830d827ac56690c506714001e06a2`. Its exact two-file candidate
resolved Next.js 15.5.21, eslint-config-next 15.5.21 and Sharp 0.35.3. Clean
install, native Sharp/libvips smoke, preflight 4/4, configuration 5/5, runner
21/21, root and package tests 92/92 and lint passed.

Before W5 was committed, the mandatory live audit newly reported reviewed
`GHSA-6g55-p6wh-862q` / `CVE-2026-45623` against the retained
`next -> postcss 8.5.10` override:

- severity: high;
- affected: PostCSS <=8.5.11;
- first fixed: 8.5.12;
- published to the GitHub Advisory Database on 2026-07-23; and
- impact: attacker-controlled source-map annotations can drive arbitrary local
  file reads, limited content disclosure and file-existence probing.

This is another advisory-database drift event, not an application-source
regression. W5 was not committed. Its exact candidate and generated outputs are
preserved recoverably at:

`C:\Users\danie\AppData\Local\Temp\blockxone-w5-postcss-audit-failure-35bd2afbc3b0485fa568a0f80a8d3458`.

The repository was restored to exact clean P6 before this plan.

## Final isolated canonical resolution

The retained simulation at:

`C:\Users\danie\AppData\Local\Temp\blockxone-web-audit-sim-9b322bccfa204bf8ac84c246c7dfa988`

was updated to current PostCSS 8.5.22 and proved:

- `next` 15.5.20 -> 15.5.21;
- `eslint-config-next` 15.5.20 -> 15.5.21;
- `overrides.next.sharp` -> 0.35.3;
- `overrides.next.postcss` 8.5.10 -> 8.5.22;
- exactly one Sharp resolution at 0.35.3;
- bundled libvips 8.18.3 and successful in-memory image creation/metadata;
- the Next env/ESLint-plugin/SWC closure at 15.5.21;
- Sharp platform packages at 0.35.3 and libvips packages at 1.3.2;
- nested Next PostCSS exactly 8.5.22;
- 42 changed package-lock entries including the root entry;
- clean `npm ci --ignore-scripts`; and
- full and runtime audits exit 0 with zero findings.

Final isolated SHA-256 values:

- `package.json`:
  `C97792E078567B2A6B756EF1D765DC2841BC8276BCB9606A5898F7A560FAD268`
- `package-lock.json`:
  `61CD695D0C31ADBB7D1BB2BF21072D3DC49F48D82FD61CBA29E9DF7A53E98EE2`

These are planning inputs, not an accepted repository change.

## Narrow supersession

This plan supersedes only these Plan 00-07 assumptions:

- the current-phase plan set becomes `00-01-PLAN.md` through
  `00-08-PLAN.md`;
- `STATE.progress.total_plans` becomes 8;
- one recognized `00-04-SUMMARY.md` produces GSD artifact progress 13%, not
  14%, because the pinned tool applies `Math.round(1 / 8 * 100)`;
- the exact web candidate includes PostCSS 8.5.22;
- accepted ancestry becomes `P6 -> P7 -> W6 -> I3 -> R3 -> V3`; and
- `W6..I3`, not uncreated W5, is the exact six-file I3 comparison.

Every other Plan 00-04 through 00-07 control, retained blocker, non-claim, role
separation, immutable boundary and final NO-GO status remains binding.

ROADMAP, REQUIREMENTS, global GSD tooling and Plans 00-04 through 00-07 are
immutable. No `00-05-SUMMARY.md` through `00-08-SUMMARY.md` may be created.

<tasks>

<task type="auto" category="ops" id="00-08-01">
  <name>Task 1: Freeze and independently accept plan-only P7</name>
  <files>none; external GEN3-P7-POSTCSS-PLAN-ACCEPTANCE.md only</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-08-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-07-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-06-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-05-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-PLAN.md,
    .planning/TEST-CONTRACT.md,
    .planning/scripts/validate-planning.ps1,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    apps/web/package.json,
    apps/web/package-lock.json
  </read_first>
  <action>
    Execution owners: controller and independent plan checker.

    Define P6 as
    `5a489930296830d827ac56690c506714001e06a2` with tree
    `dd3b3b4da15c47a0777312f0af27ebad27979921`. Commit this plan and no other
    path as P7 with message
    `docs(phase-00): accept PostCSS advisory remediation`.

    Prove `parent(P7)=P6` and `P6..P7` equals only
    `.planning/phases/00-planning-truth-and-containment/00-08-PLAN.md`.
    Prove one branch, zero remotes/tags/replace refs/grafts/alternates, strict
    fsck, clean status and no generated path.

    Write external `GEN3-P7-POSTCSS-PLAN-ACCEPTANCE.md` under the established
    Generation 3 evidence directory. Bind P6/P7 commits/trees, plan and final
    candidate hashes, advisory identity, 2026-07-23 authority, checker verdict,
    nine-path Plan 00-08 allowlist and Plans 00-04 through 00-08 union.
  </action>
  <verify>
    Independent plan checker returns VERIFICATION PASSED; P7 is plan-only;
    receipt hash is recorded; no production action occurs.
  </verify>
  <acceptance_criteria>
    - `parent(P7)` is exact P6 and `P6..P7` is only Plan 00-08.
    - Plan 00-08 names exactly nine repository paths.
    - Immutable files and prior plans are absent from its allowlist.
    - Receipt states local-only, Phase 0 in_progress, eight plans, zero
      production progress and NO-GO.
    - No unapproved summary exists.
  </acceptance_criteria>
  <done>P7 is an independently checked plan-only descendant with exact scope.</done>
  <commit>docs(phase-00): accept PostCSS advisory remediation</commit>
</task>

<task type="auto" category="code" id="00-08-02">
  <name>Task 2: Commit the final two-file web dependency remediation as W6</name>
  <files>apps/web/package.json, apps/web/package-lock.json</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-08-PLAN.md,
    .planning/TEST-CONTRACT.md,
    apps/web/package.json,
    apps/web/package-lock.json,
    apps/web/next.config.js
  </read_first>
  <action>
    Execution owner: web dependency remediator who did not check this plan.

    At exact clean P7, use Node 22.23.1/npm 10.9.8. Update package.json
    exactly:

    - `next` -> 15.5.21;
    - `eslint-config-next` -> 15.5.21;
    - `overrides.next.sharp` -> 0.35.3; and
    - `overrides.next.postcss` -> 8.5.22.

    Generate the lock canonically using package-lock-only, `--ignore-scripts`,
    `--no-audit` and `--fund=false`. Require exact equality to the final
    isolated hashes above. Reject any extra manifest, dependency, source or
    generated-file change.

    Run clean `npm ci --ignore-scripts`; require exact single Sharp 0.35.3,
    nested Next PostCSS 8.5.22, Next/env/SWC 15.5.21 and matching
    eslint-config/plugin 15.5.21.

    Run the native in-memory Sharp/libvips image smoke. Run preflight/config/
    runner/root/package suites with nonzero counts, lint, full and runtime
    audits, production build and live containment. Run both audits once before
    build and once after containment to bind the same live advisory state.
    Every audit must exit zero with zero findings.

    Commit exactly the two package files as W6 with message
    `fix(web): remediate July 2026 dependency advisories`.

    At exact clean W6, repeat resolution, native smoke, all web suites, lint,
    pre/post-build audits, production build and live containment before
    recoverably quarantining generated output.
  </action>
  <verify>
    `parent(W6)=P7`; `P7..W6` is exactly package.json/package-lock.json;
    hashes match; all precommit and exact-W6 web/audit/build gates pass; clean
    status, zero remotes and no generated path remain.
  </verify>
  <acceptance_criteria>
    - No application/test source changes.
    - Exact Next/ESLint 15.5.21, Sharp 0.35.3 and PostCSS 8.5.22.
    - Native Sharp/libvips smoke passes.
    - 4/4, 5/5, 21/21 and both 92/92 suites pass.
    - Lint, four audits per boundary, build and containment pass.
    - W6 changes exactly two paths and ends clean.
  </acceptance_criteria>
  <done>W6 is a clean two-file web remediation with zero live audit findings.</done>
  <commit>fix(web): remediate July 2026 dependency advisories</commit>
</task>

<task type="auto" category="docs" id="00-08-03">
  <name>Task 3: Run the complete matrix, freeze six-file I3 and prove exact I3</name>
  <files>
    .planning/scripts/validate-planning.ps1,
    .planning/EVIDENCE-REGISTER.md,
    .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md,
    .planning/BLOCKERS.md,
    .planning/STATE.md,
    .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-08-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-07-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-06-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-05-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-PLAN.md,
    .planning/TEST-CONTRACT.md,
    .planning/scripts/validate-planning.ps1,
    .planning/EVIDENCE-REGISTER.md,
    .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md,
    .planning/BLOCKERS.md,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Execution owner: validator/evidence builder who authored neither this plan
    nor its independent verdict.

    At exact clean W6, run every TEST-CONTRACT and bytecode-evidence command
    with pinned toolchains. Include native Sharp smoke and pre/post-build full
    and runtime web audits. Missing, skipped or zero-test suites are fatal.

    Apply the path-specific validator design with final supersessions:

    1. Exact plan set is `00-01-PLAN.md` through `00-08-PLAN.md`.
    2. Only `00-04-SUMMARY.md` is recognized; reject every other summary/name/
       case variant/addition.
    3. `init milestone-op.completed_phases == summaryPresent` 0 or 1 and is
       labeled only as a summary-bearing-phase artifact counter.
    4. Phase 0 roadmap analysis requires `plan_count=8`,
       `summary_count=summaryPresent`, `completed_phases=0`,
       `current_phase='0'`, and either planned/0 without summary or partial/13
       with the summary.
    5. Require exact Plan 00-05 builder-summary metadata and NO-GO/non-claims.
    6. STATE remains in_progress with total plans 8 and all completed values 0.
    7. ROADMAP Phase 0 remains unchecked/in progress.
    8. REQUIREMENTS contains exactly 89 unchecked unique IDs and zero checked.

    Mutation-test wrong/missing/extra/case-variant plans and summaries, wrong
    summary metadata, completed STATE/ROADMAP and checked/duplicate/missing
    requirements. Accept exact no-summary W6 and exact-one-summary I3 only.

    Recreate only the five builder-evidence files. Record all three
    advisory-drift stops, P5/D5/P6/P7/W6 ancestry, exact dependency deltas,
    W6/I3 results, counts, warnings, hashes and retained NO-GO blockers. Set
    STATE total_plans=8 and keep completion zero. ROADMAP and REQUIREMENTS stay
    byte-identical.

    Commit exactly the validator and five evidence paths as I3 with
    `docs(phase-00): record generation 3 local matrix`.

    At exact I3, rerun the complete TEST-CONTRACT, native smoke, extra audits
    and bytecode validation. Write `TASK6-I3-BUILDER-RECEIPT.md` externally,
    binding P4/P5/D5/P6/P7/W6/I3, commands/exits/counts/warnings/selections/
    hashes/generated absence. Quarantine exact generated output recoverably.
  </action>
  <verify>
    Full matrix passes at W6 and I3; validator/mutations pass; `W6..I3` is
    exactly six allowed paths; immutable files match; clean and zero-remotes.
  </verify>
  <acceptance_criteria>
    - W6 and I3 mandatory commands exit zero with nonzero tests.
    - `W6..I3` is exactly the validator plus five evidence paths.
    - Validator accepts only 0-summary/0%-planned W6 and
      1-summary/13%-partial I3 artifact states.
    - STATE remains in_progress with 8 total and 0 completed plans.
    - ROADMAP/REQUIREMENTS remain byte-identical and all 89 are unchecked.
    - Summary is builder-only and NO-GO.
    - Exact I3 is clean, remote-free and generated-path-free.
  </acceptance_criteria>
  <done>I3 is a clean six-file freeze over remediated Go and web dependencies.</done>
  <commit>docs(phase-00): record generation 3 local matrix</commit>
</task>

<task type="auto" category="verify" id="00-08-04">
  <name>Task 4: Resume independent R3, V3 and local C3 admission</name>
  <files>none beyond Plan 00-04 Task 7 and Task 8 one-file reports</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-08-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-07-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-06-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-05-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md,
    .planning/TEST-CONTRACT.md,
    .planning/BLOCKERS.md
  </read_first>
  <action>
    Three distinct agents who authored neither the dependency/evidence tree nor
    each other's report resume Plan 00-04 Tasks 7-9 with Plans 00-05 through
    00-08 binding.

    Task 7 commits only SECURITY-REVIEW as R3 with `parent(R3)=I3`. Task 8
    reruns the complete matrix and commits only VERIFICATION as V3 with
    `parent(V3)=R3`. Task 9 reruns the complete matrix and final admission at
    exact V3, writes only external `CANDIDATE-MANIFEST-v3.md`, and defines
    C3=V3.

    Use the Plans 00-04 through 00-08 union allowlist, re-snapshot C2 and retain
    Phase 0 in_progress, total plans 8, zero completed values and NO-GO.
  </action>
  <verify>
    Independent identities; exact I3/R3/V3 binding; one-file report diffs;
    exact-V3 matrix; union/immutable/topology/object/scanner/C2 checks; zero
    remotes.
  </verify>
  <acceptance_criteria>
    - No unresolved critical/high, dependency or false-success finding.
    - `I3..R3` is only SECURITY-REVIEW and `R3..V3` only VERIFICATION.
    - Exact V3 passes every test and admission command.
    - External manifest binds P3/D4/P4/P5/D5/P6/P7/W6/I3/R3/V3/C3.
    - Manifest states local-only, Phase 0 in_progress/NO-GO and no production
      action.
  </acceptance_criteria>
  <done>Generation 3 is independently reviewed, verified and locally admitted.</done>
</task>

</tasks>

<verification>

- [ ] P7 is a plan-only child of exact P6.
- [ ] W6 is a two-file web-dependency child of P7.
- [ ] Next.js, Sharp and PostCSS findings are absent at W6/I3/R3/V3.
- [ ] Native Sharp 0.35.3/libvips 8.18.3 smoke passes.
- [ ] Current phase contains eight plans and one permitted summary.
- [ ] GSD 0/1 and 0/13 artifact counters are never production progress.
- [ ] STATE remains in_progress with eight total and zero completed plans.
- [ ] ROADMAP Phase 0 and all 89 requirements remain unchecked.
- [ ] Complete matrix passes at W6, I3, R3 and exact V3.
- [ ] I3/R3/V3 parent and exact-path contracts pass.
- [ ] No unapproved summary or production/external action occurs.

</verification>

<success_criteria>

- P7 has independent plan acceptance and an external receipt.
- W6 contains only the final canonical web package resolution.
- I3 contains complete builder evidence without production-completion claims.
- R3/V3 retain independent one-file evidence ancestry.
- C3=V3 is admitted locally while Phase 0 remains in_progress and NO-GO.

</success_criteria>

<failure_and_rollback>

- Any extra dependency/path, source change, summary, completed state, audit
  finding, native-load failure, matrix failure, stale evidence or
  non-independent verdict is a hard stop.
- Never amend P6, P7, W6, I3, R3, V3 or admitted C3.
- Preserve failed attempts/receipts and quarantine only exact generated paths.

</failure_and_rollback>

<output>

Repository outputs are limited to this plan, the final two web package files,
the six-file I3 freeze and Plan 00-04 reviewer/verifier reports. External
receipts remain under:

`C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721`.

</output>
