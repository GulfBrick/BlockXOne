---
phase: 00-planning-truth-and-containment
plan: 09
type: execute
name: generation 3 clean-baseline npm lock supersession
status: proposed
gap_closure: true
wave: 1
depends_on: []
autonomous: true
risk_class: critical
requirements: [BASE-03, BASE-04, BASE-06, BASE-07]
user_setup: []
execution_mode: controller-dispatched-serial-tasks
requirements_completion: forbidden
files_modified:
  - .planning/phases/00-planning-truth-and-containment/00-09-PLAN.md
  - apps/web/package.json
  - apps/web/package-lock.json
  - .planning/scripts/validate-planning.ps1
  - .planning/EVIDENCE-REGISTER.md
  - .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md
  - .planning/BLOCKERS.md
  - .planning/STATE.md
  - .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md
must_haves:
  truths:
    - "The accepted web lock is generated by default npm 10.9.8 from the exact clean baseline, not by a stateful prior install."
    - "The July 2026 Next.js, Sharp and PostCSS findings are absent before I3."
    - "The dependency change remains exactly two package files and contains no application-source change."
    - "The current phase contains exactly nine plans while authoritative Phase 0 progress remains zero and NO-GO."
    - "The complete matrix passes at exact W7 and again at exact I3."
  artifacts:
    - "apps/web/package.json"
    - "apps/web/package-lock.json"
    - ".planning/scripts/validate-planning.ps1"
    - ".planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md"
  key_links:
    - "P8 is a plan-only child of P7; W7 is a two-file web-dependency child of P8."
    - "I3 is a six-file evidence child of W7; R3/V3 retain one-file report ancestry."
    - "Plans 00-04 through 00-09 form the Generation 3 union allowlist."
---

# Plan 00-09: Generation 3 clean-baseline npm lock supersession

## Objective

Replace Plan 00-08's stateful simulation lock with the deterministic default
npm 10.9.8 lock generated from exact clean P7, then complete the web and
platform gates without changing application source.

## Authority boundary

The user's 2026-07-23 approval covers continued local planning, exact
dependency resolution, tests, evidence, independent review, independent
verification and local commits. It does not authorize any remote, push,
deployment, RPC, provider, wallet, key, funds, real assets, production network,
production configuration, professional approval or irreversible action.

## Trigger and root cause

Plan 00-08 was accepted as P7
`c8c41dda2b5df129201a34b55c882743fa523b82`, tree
`05ea2cc9a5726f33317d8ac7fa3ee35aa5306a63`.

At exact clean P7, the approved package manifest was applied and default
Node 22.23.1/npm 10.9.8 ran:

`npm install --package-lock-only --ignore-scripts --no-audit --fund=false`

The command succeeded but correctly produced a different lock from Plan 00-08:
npm hoisted patched PostCSS 8.5.22 to the root and removed the redundant nested
Next copy. Plan 00-08 required the stateful simulation's nested lock shape, so
Task 2 stopped before smoke, tests, audits, build or commit. No W6 exists.

The clean-baseline candidate and cache are preserved at:

`C:\Users\danie\AppData\Local\Temp\blockxone-w6-default-canonical-hard-stop-6f2e50aaa9b24d018692c39da7c959b6`.

P7 was restored clean, generated-path-free and remote-free. This is a planning
determinism defect, not a product or package-resolution failure.

## Confirmed clean-baseline candidate

The exact default candidate is:

- package.json SHA-256:
  `C97792E078567B2A6B756EF1D765DC2841BC8276BCB9606A5898F7A560FAD268`
- package-lock.json SHA-256:
  `B3C27A7F07B2766ECB64A57F9357E5FC977083DD60273FC08D06E988BD60A6D3`
- Next.js/env/SWC: 15.5.21;
- eslint-config-next/plugin: 15.5.21;
- exactly one Sharp: 0.35.3;
- bundled libvips: 8.18.3;
- root PostCSS: 8.5.22;
- nested `next/node_modules/postcss`: absent;
- 43 changed lock entries including root, with two added and one removed;
- clean `npm ci --ignore-scripts`: pass;
- native in-memory Sharp create/metadata smoke: pass; and
- two consecutive full/runtime audit rounds: exit 0, zero findings.

The package resolutions close the reviewed July 2026 Next.js, Sharp and
PostCSS findings documented by Plans 00-07 and 00-08.

## Narrow supersession

This plan supersedes only:

- Plan 00-08's lock hash and nested-PostCSS expectation;
- the current-phase plan set, now exactly `00-01-PLAN.md` through
  `00-09-PLAN.md`;
- `STATE.progress.total_plans`, now 9;
- one recognized summary's GSD artifact progress, now 11% because the pinned
  tool applies `Math.round(1 / 9 * 100)`;
- ancestry, now `P7 -> P8 -> W7 -> I3 -> R3 -> V3`; and
- the six-file evidence comparison, now `W7..I3`.

All other Plan 00-04 through 00-08 controls, blockers, non-claims, role
separation and NO-GO status remain binding. ROADMAP, REQUIREMENTS, global GSD
tooling and prior plans are immutable. No `00-05-SUMMARY.md` through
`00-09-SUMMARY.md` may exist.

<tasks>

<task type="auto" category="ops" id="00-09-01">
  <name>Task 1: Freeze and independently accept plan-only P8</name>
  <files>none; external GEN3-P8-CANONICAL-NPM-PLAN-ACCEPTANCE.md only</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-09-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-08-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-07-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-06-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-05-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-PLAN.md,
    .planning/TEST-CONTRACT.md,
    .planning/scripts/validate-planning.ps1,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    apps/web/package.json,
    apps/web/package-lock.json
  </read_first>
  <action>
    Execution owners: controller and independent plan checker.

    Commit this plan and no other path as P8 with message
    `docs(phase-00): accept canonical npm lock remediation`.
    Require `parent(P8)=P7` and `P7..P8` equals only Plan 00-09.

    Prove one branch, zero remotes/tags/replace refs/grafts/alternates, strict
    fsck, clean status and no generated path. Write external
    `GEN3-P8-CANONICAL-NPM-PLAN-ACCEPTANCE.md`, binding P7/P8 commits/trees,
    plan/candidate hashes, checker verdict, authority, nine-path Plan 00-09
    allowlist and Plans 00-04 through 00-09 union semantics.
  </action>
  <verify>
    Independent checker returns VERIFICATION PASSED; P8 is plan-only; external
    receipt hash recorded; no production action.
  </verify>
  <acceptance_criteria>
    - P8 is an exact plan-only child of P7.
    - Plan 00-09 names exactly nine paths and excludes every immutable path.
    - Receipt states local-only, Phase 0 in_progress, nine plans, zero
      production progress and NO-GO.
    - No unapproved summary exists.
  </acceptance_criteria>
  <done>P8 is an independently checked plan-only descendant.</done>
  <commit>docs(phase-00): accept canonical npm lock remediation</commit>
</task>

<task type="auto" category="code" id="00-09-02">
  <name>Task 2: Commit the clean-baseline two-file web remediation as W7</name>
  <files>apps/web/package.json, apps/web/package-lock.json</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-09-PLAN.md,
    .planning/TEST-CONTRACT.md,
    apps/web/package.json,
    apps/web/package-lock.json,
    apps/web/next.config.js
  </read_first>
  <action>
    Execution owner: web dependency remediator who did not check this plan.

    At exact clean P8, prepend the exact Node 22.23.1 binary directory to PATH
    and use npm 10.9.8. Apply only:

    - next 15.5.21;
    - eslint-config-next 15.5.21;
    - overrides.next.sharp 0.35.3; and
    - overrides.next.postcss 8.5.22.

    Generate the lock from clean P8 with the exact default command above.
    Require byte equality to the two confirmed clean-baseline hashes. Reject
    every extra repository path or resolution.

    Run clean `npm ci --ignore-scripts`; assert the exact resolutions, exactly
    one Sharp and no nested PostCSS. Run the native Sharp/libvips image smoke.
    Run preflight 4/4, config 5/5, runner 21/21, root/package 92/92, lint,
    full/runtime audits, production build, containment, then full/runtime
    audits again. Every audit must exit zero.

    Commit exactly the two package files as W7 with message
    `fix(web): remediate July 2026 dependency advisories`.

    At exact W7, repeat clean install, resolution/native smoke, all web suites,
    lint, pre/post-build audits, build and containment. Quarantine generated
    output recoverably and end clean/remote-free.
  </action>
  <verify>
    `parent(W7)=P8`; `P8..W7` is exactly the package files; hashes match; every
    precommit and exact-W7 gate passes; strict fsck/clean/zero-remotes.
  </verify>
  <acceptance_criteria>
    - No source/test change and exactly two committed paths.
    - Exact Next/ESLint 15.5.21, Sharp 0.35.3, PostCSS 8.5.22.
    - Native smoke, 4/4, 5/5, 21/21, both 92/92, lint, audits, build and
      containment pass before and after commit.
    - W7 ends clean with no generated path.
  </acceptance_criteria>
  <done>W7 is the canonical clean-baseline web remediation.</done>
  <commit>fix(web): remediate July 2026 dependency advisories</commit>
</task>

<task type="auto" category="docs" id="00-09-03">
  <name>Task 3: Run the complete matrix, freeze I3 and prove exact I3</name>
  <files>
    .planning/scripts/validate-planning.ps1,
    .planning/EVIDENCE-REGISTER.md,
    .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md,
    .planning/BLOCKERS.md,
    .planning/STATE.md,
    .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-09-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-08-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-07-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-06-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-05-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-PLAN.md,
    .planning/TEST-CONTRACT.md,
    .planning/scripts/validate-planning.ps1,
    .planning/EVIDENCE-REGISTER.md,
    .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md,
    .planning/BLOCKERS.md,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Execution owner: evidence builder who authored neither plan nor verdict.

    At exact clean W7, run every TEST-CONTRACT and bytecode command, plus the
    native Sharp smoke and repeated web audits. Missing/skipped/zero-test
    suites are fatal.

    Update the path-specific validator to:

    1. require exactly `00-01-PLAN.md` through `00-09-PLAN.md`;
    2. recognize only `00-04-SUMMARY.md` and reject every other name/case/addition;
    3. require GSD init's summary-bearing-phase counter to equal summaryPresent
       0 or 1 without calling it production progress;
    4. require Phase 0 plan_count=9, summary_count=summaryPresent,
       completed_phases=0, current_phase=0, and planned/0 without summary or
       partial/11 with it;
    5. enforce exact builder-summary metadata, empty completed requirements,
       in_progress/NO-GO and mandatory Tasks 7-9 non-claims;
    6. enforce STATE in_progress, total_plans=9 and completed values zero;
    7. enforce unchecked/in-progress Phase 0 ROADMAP; and
    8. enforce exactly 89 unchecked unique requirements and zero checked.

    Mutation-test all filename, metadata, completion and requirements
    violations. Accept exact no-summary W7 and one-summary I3 only.

    Recreate only the five builder-evidence files. Record every advisory and
    lock-determinism stop, P5/D5/P6/P7/P8/W7 ancestry, exact dependencies,
    W7/I3 results, warnings/hashes and all retained NO-GO blockers. Keep
    ROADMAP/REQUIREMENTS byte-identical and STATE completion zero.

    Commit exactly the validator and five evidence paths as I3 with
    `docs(phase-00): record generation 3 local matrix`. At exact I3 rerun the
    full matrix/native smoke/repeated audits/bytecode proof. Write the external
    builder receipt bound to all ancestry and quarantine generated output.
  </action>
  <verify>
    W7/I3 matrices and mutations pass; `W7..I3` is exactly six paths;
    immutable equality, clean status and zero remotes.
  </verify>
  <acceptance_criteria>
    - Every W7/I3 gate exits zero with nonzero tests.
    - Validator accepts only W7 planned/0 and I3 partial/11 artifact states.
    - STATE is in_progress with 9 total and 0 completed plans.
    - ROADMAP/REQUIREMENTS are unchanged and all 89 unchecked.
    - Summary remains builder-only/NO-GO.
    - I3 is clean, remote-free and generated-path-free.
  </acceptance_criteria>
  <done>I3 is a clean six-file freeze over canonical dependency remediations.</done>
  <commit>docs(phase-00): record generation 3 local matrix</commit>
</task>

<task type="auto" category="verify" id="00-09-04">
  <name>Task 4: Resume independent R3, V3 and local C3 admission</name>
  <files>none beyond Plan 00-04 Task 7 and Task 8 one-file reports</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-09-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-08-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-07-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-06-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-05-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md,
    .planning/TEST-CONTRACT.md,
    .planning/BLOCKERS.md
  </read_first>
  <action>
    Three distinct independent agents resume Plan 00-04 Tasks 7-9 with all
    superseding plans binding. Task 7 commits only SECURITY-REVIEW as R3 with
    parent I3. Task 8 reruns the full matrix and commits only VERIFICATION as
    V3 with parent R3. Task 9 reruns full matrix/final admission at V3, writes
    only external CANDIDATE-MANIFEST-v3.md, and defines C3=V3.

    Use the Plans 00-04 through 00-09 union; re-snapshot C2; retain Phase 0
    in_progress, total plans 9, zero completed values and NO-GO.
  </action>
  <verify>
    Independent identities; exact one-file ancestry; exact-V3 matrix; union,
    immutable, topology, object, scanner and C2 checks; zero remotes.
  </verify>
  <acceptance_criteria>
    - No unresolved critical/high, dependency or false-success finding.
    - I3..R3 and R3..V3 are the exact one-file reports.
    - Exact V3 passes every test/admission command.
    - External manifest binds all accepted ancestry through C3=V3 and states
      local-only, in_progress/NO-GO and no production action.
  </acceptance_criteria>
  <done>Generation 3 is independently reviewed, verified and locally admitted.</done>
</task>

</tasks>

<verification>

- [ ] P8 is plan-only over P7 and W7 is exactly two package files over P8.
- [ ] Clean-baseline lock hash and all exact dependency resolutions match.
- [ ] Native smoke, all web/platform gates and repeated audits pass.
- [ ] Current phase has nine plans and one permitted summary.
- [ ] GSD 0/1 and 0/11 artifact counters are never production progress.
- [ ] STATE/ROADMAP/89 requirements remain authoritative and incomplete.
- [ ] W7/I3/R3/V3 matrices and exact parent/path contracts pass.
- [ ] No unapproved summary or external/production action occurs.

</verification>

<success_criteria>

- P8 is independently accepted with an external receipt.
- W7 is the deterministic clean-baseline web package commit.
- I3/R3/V3/C3 retain complete, independent, local-only evidence ancestry.
- Phase 0 remains in_progress and release remains NO-GO.

</success_criteria>

<failure_and_rollback>

- Any extra path/resolution, audit/smoke/test/build failure, completed state,
  stale evidence or role failure is a hard stop.
- Never amend P7, P8, W7, I3, R3, V3 or admitted C3.
- Preserve failed candidates and quarantine exact generated paths recoverably.

</failure_and_rollback>

<output>

Repository outputs are limited to this plan, two web package files, six-file
I3 and the original reviewer/verifier reports. External receipts remain under:

`C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721`.

</output>
