---
phase: 00-planning-truth-and-containment
plan: 04
type: execute
name: clean-lineage generation 3 gap closure
status: proposed
gap_closure: true
wave: 1
depends_on: []
autonomous: true
risk_class: critical
requirements: [BASE-03, BASE-04, BASE-06, BASE-07]
user_setup: []
execution_mode: controller-dispatched-serial-tasks
requirements_completion: forbidden
files_modified:
  - .planning/phases/00-planning-truth-and-containment/00-04-PLAN.md
  - .planning/APPROVALS.md
  - .planning/DECISIONS.md
  - .planning/BLOCKERS.md
  - .planning/STATE.md
  - .planning/TEST-CONTRACT.md
  - .planning/EVIDENCE-REGISTER.md
  - .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md
  - .planning/phases/00-planning-truth-and-containment/00-SECURITY-REVIEW.md
  - .planning/phases/00-planning-truth-and-containment/00-VERIFICATION.md
  - .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md
  - .github/workflows/ci.yml
  - apps/web/package.json
  - apps/web/vitest.config.ts
  - apps/web/scripts/run-hermetic-tests.mjs
  - apps/web/scripts/run-hermetic-tests.node-test.mjs
  - apps/web/scripts/test-production-containment.mjs
  - apps/web/src/test/hermetic-network-guard.ts
  - apps/web/src/test/hermetic-network-guard.test.ts
  - contracts/hardhat.config.ts
  - contracts/scripts/preflight.test.mjs
  - contracts/scripts/capture-bytecode-manifest.mjs
  - contracts/scripts/compare-bytecode-manifests.mjs
  - contracts/src/identity/IdentityRegistry.sol
  - contracts/src/token/BXOSecurityToken.sol
  - contracts/src/token/BXOSecurityTokenFactory.sol
  - contracts/src/mocks/MockClaimIssuer.sol
  - contracts/src/mocks/MockGovernanceExecutor.sol
  - contracts/src/mocks/MockRegistryFailures.sol
  - contracts/test/IdentityRegistry.test.ts
  - contracts/test/BXOSecurityToken.test.ts
  - contracts/test/BXOSecurityTokenFactory.test.ts
  - contracts/ERC3643_IMPLEMENTATION.md
  - contracts/MANIFEST.md
must_haves:
  truths:
    - "C2 stays immutable, clean and remote-free while Generation 3 remains an ordinary descendant."
    - "Solidity stays at 0.8.20 with optimizer 200 and an explicit Shanghai build target, with exact configuration-only bytecode equality."
    - "Web unit tests use deterministic discovery, a minimal inherited environment and a tested fetch-level non-loopback guard."
    - "Identity verification fails closed unless every required topic has a current authorized issuer and a cryptographically accepted claim."
    - "Standard and forced issuance share one non-reentrancy boundary and both require registration, verified identity, configured compliance and a successful compliance decision."
    - "Forced issuance is separate, contract-held, evidence-bound and replay-resistant, with no eligibility bypass."
    - "Independent security review, verification and final admission bind to frozen commits without self-referential verdict claims."
  artifacts:
    - ".planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md"
    - ".planning/phases/00-planning-truth-and-containment/00-SECURITY-REVIEW.md"
    - ".planning/phases/00-planning-truth-and-containment/00-VERIFICATION.md"
    - "contracts/scripts/capture-bytecode-manifest.mjs"
    - "apps/web/scripts/run-hermetic-tests.mjs"
    - "contracts/test/IdentityRegistry.test.ts"
  key_links:
    - "BXOSecurityToken issuance entry points -> authorization-free eligibility helper -> IdentityRegistry + ModularCompliance"
    - "IdentityRegistry required topics -> trusted issuer topic authorization -> IClaimIssuer.isClaimValid"
    - "web npm test -> hermetic runner -> local Vitest module -> explicit Vitest config + fetch guard"
    - "C2 -> D3 -> P3 -> implementation freeze I3 -> review commit R3 -> verification commit V3/C3"
---

<objective>
Produce a locally admitted Generation 3 descendant of clean candidate C2 that hardens compiler truth, web-test containment, identity verification and issuance without claiming Phase 0, G5, ERC-3643 conformance or production readiness.

Purpose: close critical fail-open behavior early while preserving the clean-root evidence boundary and every professional, hosted, chain-selection and irreversible-action gate.

Output: atomic local commits, external evidence bound to exact commits, independent adversarial and verification reports, and one clean local candidate C3 with zero remotes.
</objective>

<execution_context>
@C:/Users/danie/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.codex/get-shit-done/templates/summary.md
@C:/Users/danie/.codex/skills/blockxone-blockchain-readiness/SKILL.md

This is a controlled exception to the stock phase finalizer. The controller dispatches each task in document order to the named role and verifies each atomic commit. It must not run `requirements mark-complete`, `roadmap update-plan-progress`, phase completion or auto-advance. This packet addresses evidence relevant to the listed BASE requirements but does not complete them. ROADMAP.md, REQUIREMENTS.md and their checkboxes are immutable.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/00-planning-truth-and-containment/00-RESEARCH.md
@docs/BLOCKXONE_AGENT_EXECUTION_LOOP.md
</context>

# Plan 00-04: Clean-Lineage Generation 3 Gap Closure

## Control contract

| Field | Value |
| --- | --- |
| Admitted baseline | `C2 = a88658ad82f3d22aaf26e10b9eab6389084e6dd3` |
| Research commit | `D3 = d0c71d7919a6ffa4a18ef3e0fe62f41fb9867294`; its parent must equal C2 |
| Working repository | `C:\Users\danie\Documents\BlockXOne Production Gen3` |
| Branch | `codex/phase-00-generation-3` |
| Immutable C2 repository | `C:\Users\danie\Documents\BlockXOne Production Clean v2` |
| External evidence directory | `C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721`; it must be absent before acceptance |
| Plan commit | `P3`; resolve with `git rev-parse HEAD` immediately after committing only this accepted plan |
| Implementation freeze | `I3`; exact builder-evidence commit before independent review |
| Review commit | `R3`; parent I3, changes only `00-SECURITY-REVIEW.md` |
| Verification/final candidate | `V3 = C3`; parent R3, changes only `00-VERIFICATION.md` |
| Retry ceiling | Two remediation cycles for the same typed root cause |
| Remote authority | None; zero remotes and no push |

The external directory has deterministic filenames: `GEN3-PLAN-ACCEPTANCE.md`, `bytecode-baseline-c2.json`, `bytecode-explicit-shanghai.json`, `bytecode-comparison.json`, `SECURITY-REVIEW-RECEIPT.md`, `VERIFICATION-RECEIPT.md`, and `CANDIDATE-MANIFEST-v3.md`.

## Approved local policy

The user approved local implementation on 2026-07-21:

- Standard minting is allowed only to a nonzero, registered, cryptographically verified and compliance-eligible recipient.
- Forced issuance has a separate role and entry point, a one-time operation ID, a nonzero evidence hash and a durable audit event.
- Forced issuance uses the same eligibility policy. There is no identity, KYC/AML, sanctions, product-eligibility or compliance bypass.
- The forced-issuer role is unassigned by default and grantable only to an address with deployed code.

This is local implementation authorization. It is not Legal/Registrar/MLRO, CISO, G5, network, deployment or production approval, and it does not identify the user as a professional approver.

## Immutable boundary and non-claims

- The frontmatter `files_modified` list is the complete repository allowlist from D3 through C3. Anything else is a hard stop and requires a newly checked plan.
- The plan file is the only permitted `D3..P3` change. It must not change after P3.
- `ROADMAP.md`, `REQUIREMENTS.md`, prior plans, proof scripts, incident records and all C2 repository state are immutable.
- Builder, adversarial reviewer, verifier and final-admission agent are separate roles. A role may not verify its own edits.
- Generated dependencies, builds, artifacts, caches, coverage, TypeChain and temporary test state are never committed.
- The packet does not close compliance callbacks, broader role separation, forced-transfer/recovery, official ERC-3643 conformance, canonical stack selection, production claim policy, actual multisig design, target chain, compiler provenance, hosted CI, incident closure, professional approvals, external audit, custody or finance gates.
- Final release status stays `NO-GO` and Phase 0 stays `in_progress`.

<tasks>

<task type="auto" category="ops" id="00-04-01">
  <name>Task 1: Freeze lineage, approval meaning and execution metadata</name>
  <files>.planning/APPROVALS.md, .planning/DECISIONS.md, .planning/BLOCKERS.md, .planning/STATE.md</files>
  <read_first>.planning/phases/00-planning-truth-and-containment/00-04-PLAN.md, .planning/phases/00-planning-truth-and-containment/00-RESEARCH.md, .planning/APPROVALS.md, .planning/DECISIONS.md, .planning/BLOCKERS.md, .planning/STATE.md, C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\OPTION-A-APPROVAL-RECEIPT.md, C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\CANDIDATE-MANIFEST-v2.md</read_first>
  <action>
    Execution owner: controller. Before source edits, resolve P3 with `git rev-parse HEAD` and prove `parent(D3)=C2`, `parent(P3)=D3`, and `git diff --name-only D3..P3` is exactly this plan. Prove Gen3 has its own absolute `git-common-dir` and `git-dir`, no alternates, shared repository, shallow state, partial/promisor settings, replace refs, grafts or extra worktrees; exactly one local branch, zero tags/remotes, silent strict fsck, and equal physical/reachable object sets. Snapshot C2 HEAD, tree, refs, config hash, clean status, object counts and v2-manifest SHA-256 without writing to C2. Create the previously absent external evidence directory and write `GEN3-PLAN-ACCEPTANCE.md` with C2/D3/P3, accepted plan SHA-256, the independent checker verdict, those boundary proofs and the exact local-only approval language.

    Update the four planning registers only: record the local mint policy, explicit Solidity 0.8.20/optimizer-200/Shanghai containment decision, production network still unselected, all open gates, the Gen3 repo/branch and external evidence path. In STATE frontmatter set `progress.total_plans: 4`, keep `completed_plans: 0`, status `in_progress`, and update the timestamp. Do not alter phase or requirement completion.
  </action>
  <verify>Run planning validation; compare D3..P3 exactly; run strict Git-boundary checks on Gen3; compare fresh C2 snapshot to the v2 manifest; verify both repos have zero remotes.</verify>
  <acceptance_criteria>
    - `D3..P3` contains only `00-04-PLAN.md`, whose SHA-256 equals the acceptance receipt.
    - The receipt has no unresolved KILL, FIX or CLARIFY item.
    - APPROVALS contains `local implementation authorization` and `not Legal, CISO, G5 or production approval`.
    - STATE has total_plans 4, completed_plans 0, status in_progress and no completed requirement/phase.
    - Gen3 is isolated and C2 remains exact, clean, one-branch and remote-free.
  </acceptance_criteria>
  <done>Lineage, user authority and non-authority are exact and externally receipted before source work.</done>
  <commit>docs(phase-00): record generation 3 local policy</commit>
</task>

<task type="auto" category="code" id="00-04-02">
  <name>Task 2: Pin compiler truth and prove configuration-only bytecode equality</name>
  <files>contracts/hardhat.config.ts, contracts/scripts/preflight.test.mjs, contracts/scripts/capture-bytecode-manifest.mjs, contracts/scripts/compare-bytecode-manifests.mjs</files>
  <read_first>contracts/hardhat.config.ts, contracts/package.json, contracts/package-lock.json, contracts/scripts/preflight.test.mjs, contracts/MANIFEST.md, .planning/phases/00-planning-truth-and-containment/00-RESEARCH.md</read_first>
  <action>
    Execution owner: compiler builder. Add one capture script and one fail-closed comparison script. The capture script must enumerate every fully qualified contract from Hardhat build-info/artifacts and record source name, contract name, ABI SHA-256, complete creation and deployed bytecode SHA-256 including metadata, link/deployed-link/immutable references, solc version and longVersion, optimizer settings, EVM target, canonical compiler-input SHA-256, Solidity-source-set SHA-256, artifact-set SHA-256, package-lock SHA-256, Node/npm/Hardhat identities, bound commit/tree, and its own capture-script SHA-256. Missing, duplicate or malformed data is fatal. The comparison must reject missing/extra contracts, fields or references; no metadata stripping or normalization.

    Keep Solidity `0.8.20`, optimizer enabled with `runs: 200`, and add only `evmVersion: "shanghai"` to compiler behavior. Preflight must assert those exact values and Hardhat 3.10.0.

    Materialize C2 from Gen3 using a newly generated PowerShell variable: `$gen3TempRoot = Join-Path ([System.IO.Path]::GetTempPath()) ('blockxone-gen3-c2-' + [Guid]::NewGuid().ToString('N'))`; create that exact absent directory, resolve it beneath `[System.IO.Path]::GetTempPath()`, then run `git archive --format=tar --output="$gen3TempRoot\c2.tar" a88658ad82f3d22aaf26e10b9eab6389084e6dd3` and extract into `$gen3TempRoot\source`. Build there; never build in the admitted C2 repository. Use Node 22.23.1/npm 10.9.8 and `npm ci --ignore-scripts`. After creating the Task 2 commit, build that clean commit from empty artifacts/cache. Run the exact same capture-script bytes from the Task 2 commit for both artifact sets and write deterministic external files `bytecode-baseline-c2.json`, `bytecode-explicit-shanghai.json`, and `bytecode-comparison.json`. Equality applies only to C2 versus the compiler-configuration Task 2 commit; later Solidity/mocks intentionally change bytecode. Remove only the resolved `$gen3TempRoot` after evidence hashes are recorded.
  </action>
  <verify>`npm run test:preflight --prefix contracts`; force-clean compile and typecheck; run capture twice per artifact set and compare each pair; run the comparison script against the exact external filenames; assert identical ABI/creation/deployed bytecode and identical capture-tool hash; explicitly test missing/extra/mutated data failures.</verify>
  <acceptance_criteria>
    - Hardhat contains Solidity 0.8.20, optimizer true/200 and explicit Shanghai; preflight fails on drift.
    - Both manifests bind to exact commits/trees, lockfiles, compiler input, artifacts and the same capture-tool bytes.
    - Complete ABI, creation and deployed bytecode including metadata are byte-for-byte equal for C2 versus the Task 2 commit.
    - C2 was materialized from Gen3 and its admitted repository was never built in or changed.
    - No generated artifact/cache/compiler/dependency path is committed or left in the source worktree.
  </acceptance_criteria>
  <done>The implicit Shanghai behavior is explicit, regression-tested and proven configuration-only without bytecode drift.</done>
  <commit>build(contracts): pin shanghai without bytecode drift</commit>
</task>

<task type="auto" category="code" id="00-04-03">
  <name>Task 3: Make web tests deterministic, environment-minimal and fail closed</name>
  <files>apps/web/package.json, apps/web/vitest.config.ts, apps/web/scripts/run-hermetic-tests.mjs, apps/web/scripts/run-hermetic-tests.node-test.mjs, apps/web/scripts/test-production-containment.mjs, apps/web/src/test/hermetic-network-guard.ts, apps/web/src/test/hermetic-network-guard.test.ts, .github/workflows/ci.yml, .planning/TEST-CONTRACT.md</files>
  <read_first>apps/web/package.json, apps/web/package-lock.json, apps/web/scripts/preflight.mjs, apps/web/scripts/preflight.node-test.mjs, apps/web/scripts/production-url-policy.node-test.mjs, apps/web/scripts/test-production-containment.mjs, apps/web/next.config.js, .github/workflows/ci.yml, .planning/TEST-CONTRACT.md, contracts/hardhat.config.ts</read_first>
  <action>
    Execution owner: web-containment builder. Define web root with `fileURLToPath(new URL("..", import.meta.url))` from the runner and invoke exactly `<web-root>/node_modules/vitest/vitest.mjs` through `process.execPath`; never use npx or package downloads. Normalize legacy `--run` by removing duplicate `--run`, reject watch/UI modes, and always invoke Vitest `run`. Create a per-run cache using `fs.mkdtemp` under `os.tmpdir`, pass only that exact cache path into Vitest config, and recursively remove only the resolved child path in `finally`.

    Construct the child environment from a documented OS-minimum allowlist: on all platforms `PATH`; on Windows additionally `SystemRoot`, `WINDIR`, `COMSPEC`, `PATHEXT`, `TEMP`, `TMP`, `SystemDrive`; on POSIX additionally `HOME`, `TMPDIR`; then set only `NODE_ENV=test`, `TZ=UTC`, `LANG=C`, `LC_ALL=C`, `NEXT_PUBLIC_API_URL=https://api.blockxone.example`, `SERVER_ACTION_ALLOWED_ORIGINS=app.blockxone.example`, and the generated cache path. Never spread `process.env`. Reject forwarding names containing database, RPC, provider, signing, token, password, secret, credential, private-key or production-URL indicators.

    Configure Vitest to include only `src/**/*.test.{ts,tsx}`, exclude dependencies/build/scripts/caches/`_incoming_fullstack`, isolate tests, fail on zero tests, and load the fetch guard. The guard denies HTTP(S) except normalized `localhost`, `localhost.`, IPv4 `127.0.0.0/8`, IPv6 `::1`/bracket form; it leaves non-HTTP schemes to native fetch behavior and is documented as fetch-level protection, not an OS firewall. Runner tests inject hostile variables, compare root/package-root nonzero discovery and repeated results, test deliberate failure propagation, legacy arguments and cache cleanup.

    Give the containment probe the same minimal-environment discipline, reserved example-domain values, loopback-only host/port and `windowsHide: true`; await graceful child exit with a fixed timeout, then terminate the exact child/process group and escalate (`SIGKILL` on POSIX, exact-PID `taskkill /T /F` on Windows) with a second bounded wait. Confirm the port is closed. Update CI and TEST-CONTRACT once, including the compiler truth from Task 2 and the statement `production network remains unselected`.
  </action>
  <verify>Run Node-native runner/guard tests, web preflight/config/URL policy, root and package-root unit runs twice, deliberate failing fixture, lint, build, full/runtime audits and production containment on Node 22.23.1/npm 10.9.8; then explicitly `Test-Path` all `.next`, coverage and configured cache candidates after bounded cleanup.</verify>
  <acceptance_criteria>
    - Root and package-root commands execute the same nonzero test set twice and failure returns nonzero.
    - Hostile inherited variables are absent and runner source contains no process.env spread, npx or download path.
    - Non-loopback HTTP(S) fetches fail; normalized loopback hosts pass; documentation does not claim OS isolation.
    - Temporary cache paths and `.next`/coverage outputs are explicitly absent after cleanup, not inferred from Git status.
    - The containment child exits, its port closes, and no spawned server remains.
    - TEST-CONTRACT says Solidity 0.8.20, optimizer 200, Shanghai build target, and production network remains unselected; it contains no 0.8.24 claim.
  </acceptance_criteria>
  <done>Local and CI web-test execution has deterministic discovery, minimal environment propagation, bounded cleanup and accurate isolation claims.</done>
  <commit>test(web): make execution hermetic and fail closed</commit>
</task>

<task type="auto" category="code" id="00-04-04">
  <name>Task 4: Harden the identity predicate used by issuance</name>
  <files>contracts/src/identity/IdentityRegistry.sol, contracts/src/mocks/MockClaimIssuer.sol, contracts/src/mocks/MockRegistryFailures.sol, contracts/test/IdentityRegistry.test.ts</files>
  <read_first>contracts/src/identity/IdentityRegistry.sol, contracts/src/identity/IIdentity.sol, contracts/src/compliance/IClaimIssuer.sol, contracts/src/compliance/ITrustedIssuersRegistry.sol, contracts/src/compliance/TrustedIssuersRegistry.sol, contracts/src/compliance/ClaimTopicsRegistry.sol, contracts/src/mocks/MockClaimIssuer.sol, .planning/phases/00-planning-truth-and-containment/00-RESEARCH.md</read_first>
  <action>
    Execution owner: identity-contract builder. `isVerified` returns false for an unregistered wallet or zero required topics. Treat failure/malformed data from the registry-level `getClaimTopics` call as false. For each required topic, treat failures from `getClaimIdsByTopic`, individual `getClaim`, `isTrustedIssuer`, `getTrustedIssuerClaimTopics`, and `IClaimIssuer.isClaimValid` as claim-level rejection and continue to another claim for that topic; return false if none qualifies. A qualifying claim must have the exact topic, nonempty signature, containment scheme constant 1, a currently trusted issuer currently authorized for that exact topic, and a true Claim Issuer response. Pass identity address, topic, signature and data unchanged to `isClaimValid`.

    Make MockClaimIssuer test-only and configurable for true, false, revert, malformed return and exact-argument matching. Add test-only registry failure mocks for reverting/malformed topic and issuer-topic calls. Do not implement ECDSA recovery, expiry parsing or a production claim policy; those remain G5 decisions.
  </action>
  <verify>Force compile/typecheck; run dedicated identity tests; run the full contract suite. Mutation checks must show every predicate can independently turn a valid identity invalid.</verify>
  <acceptance_criteria>
    - Empty topics, missing topics, wrong scheme, empty signature, untrusted issuer and unauthorized issuer-topic all return false.
    - False, revert or malformed issuer validation and registry failures never create eligibility.
    - Revoking/removing issuer or topic authorization invalidates immediately.
    - Alternative claims continue after one invalid/reverting claim, but every required topic still needs one valid claim.
    - Tests prove the identity/topic/signature/data tuple reaches the Claim Issuer unchanged.
    - Production registry references IClaimIssuer but neither mock contracts nor hand-written signature recovery.
  </acceptance_criteria>
  <done>Identity verification is an explicit fail-closed cryptographic trust predicate suitable for the bounded issuance containment.</done>
  <commit>security(contracts): fail closed on invalid identity claims</commit>
</task>

<task type="auto" category="code" id="00-04-05">
  <name>Task 5: Enforce governed standard mint and separate audited forced issuance</name>
  <files>contracts/src/token/BXOSecurityToken.sol, contracts/src/token/BXOSecurityTokenFactory.sol, contracts/src/mocks/MockGovernanceExecutor.sol, contracts/test/BXOSecurityToken.test.ts, contracts/test/BXOSecurityTokenFactory.test.ts, contracts/ERC3643_IMPLEMENTATION.md, contracts/MANIFEST.md</files>
  <read_first>contracts/src/token/BXOSecurityToken.sol, contracts/src/token/BXOSecurityTokenFactory.sol, contracts/src/identity/IdentityRegistry.sol, contracts/src/compliance/IModularCompliance.sol, contracts/src/compliance/ModularCompliance.sol, contracts/src/compliance/modules/CountryRestrictionModule.sol, contracts/src/compliance/modules/MaxBalanceModule.sol, contracts/test/BXOSecurityToken.test.ts, contracts/test/BXOSecurityTokenFactory.test.ts, contracts/ERC3643_IMPLEMENTATION.md, contracts/MANIFEST.md</read_first>
  <action>
    Execution owner: token-contract builder. Inherit OpenZeppelin `ReentrancyGuard` and apply one shared `nonReentrant` boundary to `mint`, `batchMint` and `forcedIssue`. Add `MINTER_ROLE`, `FORCED_ISSUER_ROLE`, `MAX_BATCH_MINT_SIZE = 100`, replay state, `MintExecuted`, and `ForcedIssuanceExecuted`. Grant constructor admin MINTER_ROLE and grant no forced role. Implement exactly `function _grantRole(bytes32 role, address account) internal override returns (bool)`; reject `FORCED_ISSUER_ROLE` when `account.code.length == 0`, otherwise `return super._grantRole(role, account)`. Document that this proves contract-held authority only, not multisig quality.

    Put authorization and `whenNotPaused` on external entry points. The shared `_requireIssuanceEligible(recipient, amount)` must be authorization-free and require nonzero recipient/amount, `identityRegistry.contains`, hardened `isVerified`, a nonempty `compliance.getModules()` result, and successful `compliance.canTransfer(address(0), recipient, amount)`. Reverts/malformed compliance calls fail closed. This minimum-one-module rule prevents empty-compliance approval but does not approve the eventual canonical module set.

    `mint` requires MINTER_ROLE. `batchMint` requires MINTER_ROLE, rejects empty/mismatched/over-100 input, uses the same helper sequentially and reverts atomically; duplicate recipients observe updated balances. Emit MintExecuted per success. `forcedIssue(bytes32 operationId,address recipient,uint256 amount,bytes32 evidenceHash)` requires only FORCED_ISSUER_ROLE, nonzero unused ID and nonzero evidence hash; set replay state before the helper's external calls and rely on transaction rollback if eligibility fails. It uses the same helper and has no bypass.

    Add a test-only governance executor and a test-only claim-issuer reentrancy probe. The probe must attempt a different-operation-ID forced issue from a standard mint and a standard mint from a forced issue, and it must recognize the shared guard's `ReentrancyGuardReentrantCall()` selector rather than treating any arbitrary revert as proof. The factory grants caller MINTER_ROLE, uses standard mint for optional initial supply, never grants forced issuance, and renounces the factory's minter, agent and default-admin roles. Ineligible initial supply reverts atomically. Documentation names BXOSecurityToken only as this packet's reviewed prototype and lists BXOAssetToken, RestrictedSecurityToken and mocks as noncanonical pending G5, together with every open callback/role/forced-transfer/recovery/multisig/conformance blocker.
  </action>
  <verify>Force compile/typecheck; run targeted identity/mint/batch/forced/factory tests and the full contract suite; run full/runtime audits. Exercise public grantRole and direct/factory deployments.</verify>
  <acceptance_criteria>
    - Unauthorized, paused, zero, unregistered, unverified, empty-compliance, restricted-country, max-balance and rejecting/reverting compliance mints fail.
    - Valid single/batch mint passes; empty/mismatched/oversized/mixed-invalid/duplicate aggregate-limit batches revert with unchanged balances and supply.
    - Unsafe unregistered-mint test/expectation is absent.
    - Forced role has no initial holder; EOA grant rejects, contract grant accepts, MINTER_ROLE alone cannot force issue.
    - Forced issue rejects zero/reused ID, zero evidence and every ineligible recipient; event fields are exact and failed eligibility does not consume the ID.
    - Different-ID and cross-function reentry attempts fail specifically with the shared ReentrancyGuard error while the valid outer issuance remains atomic and correctly accounted.
    - Factory leaves itself no admin/agent/minter/forced role and rejects ineligible initial supply atomically.
    - Supply-invariant tests reconcile successful issuance and burns across tested sequences.
    - No eligibility bypass, production, canonical ERC-3643 or real-multisig claim appears.
  </acceptance_criteria>
  <done>Both issuance paths are fail closed, least-privilege separated, auditable and subject to the same identity/compliance eligibility.</done>
  <commit>security(contracts): enforce governed issuance eligibility</commit>
</task>

<task type="auto" category="docs" id="00-04-06">
  <name>Task 6: Run the full local matrix, write builder evidence and freeze I3</name>
  <files>.planning/EVIDENCE-REGISTER.md, .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md, .planning/BLOCKERS.md, .planning/STATE.md, .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md</files>
  <read_first>.planning/TEST-CONTRACT.md, .planning/EVIDENCE-REGISTER.md, .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md, .planning/BLOCKERS.md, .planning/STATE.md, .planning/scripts/validate-planning.ps1, .planning/scripts/validate-repository-artifacts.ps1, .planning/scripts/validate-ci-policy.ps1, .planning/scripts/validate-production-compose.ps1</read_first>
  <action>
    Execution owner: evidence builder. From a clean implementation commit, run every mandatory local command in TEST-CONTRACT using pinned toolchains: planning/repository/CI/Compose/actionlint; Go tests/vet/lint/gosec/govulncheck; web preflight/config/runner/unit/lint/build/audits/containment; contract preflight/compile/typecheck/tests/audits; exact bytecode-evidence validation. Record exact commit/tree, tool versions, commands, exits, test counts, warnings and external hashes. Explicitly prove plan allowlist containment, immutable-file equality, diff check, generated-path absence and zero remotes.

    Update only locally resolved gaps; retain hosted, incident, compliance-lifecycle, G5, audit and professional blockers. Create SUMMARY with all task commits/paths/tests/deviations. Commit it and name the resulting exact commit I3. Keep status in_progress, total_plans 4 and completed_plans 0. Do not edit reviewer/verifier files.
  </action>
  <verify>Every TEST-CONTRACT command exits zero at the frozen tree; `git diff --check`; explicit generated-path absence; `P3..I3` allowlist/immutable comparisons; clean status and zero remotes after the evidence commit.</verify>
  <acceptance_criteria>
    - No command is missing, skipped, zero-test or bound to another commit.
    - P3..I3 contains only authorized implementation/builder-evidence paths and no immutable file.
    - Evidence and summary say Phase 0 in_progress and NO-GO and retain all named gates.
    - I3 is clean and its exact commit/tree and external evidence hashes are recorded.
  </acceptance_criteria>
  <done>A clean frozen implementation commit I3 has complete builder evidence but no self-review claim.</done>
  <commit>docs(phase-00): record generation 3 local matrix</commit>
</task>

<task type="auto" category="verify" id="00-04-07">
  <name>Task 7: Adversarially review frozen implementation I3</name>
  <files>.planning/phases/00-planning-truth-and-containment/00-SECURITY-REVIEW.md</files>
  <read_first>.planning/phases/00-planning-truth-and-containment/00-04-PLAN.md, .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md, .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md, .planning/BLOCKERS.md, all P3..I3 changed source and tests</read_first>
  <action>
    Execution owner: an adversarial reviewer who authored no implementation or builder evidence. Freeze I3 and review identity failure handling, argument integrity, empty topics/compliance, issuer-topic authorization, standard/forced authorization, AccessControl override, batch atomicity, replay order, external-call reentry, factory residue, web discovery/environment/cache/egress/shutdown scope, compiler evidence and every non-claim. Run targeted adversarial tests at exact I3. Write external `SECURITY-REVIEW-RECEIPT.md` bound to I3 commit/tree and the reviewed changed-path/test hashes. Only this reviewer may update the internal security report; it must state that its verdict binds I3, not its own future commit. Commit only that report, then require parent(R3)=I3 and `I3..R3` equals only the report.
  </action>
  <verify>Independent reviewer identity/role separation; exact I3 binding; no unresolved critical/high or false-success finding; parent/path proof for R3; clean status and zero remotes.</verify>
  <acceptance_criteria>
    - Reviewer authored none of P3..I3 and receipt binds exact I3.
    - Typed findings have evidence and disposition; any unresolved critical/high returns to remediation instead of proceeding.
    - R3 changes only SECURITY-REVIEW and makes no self-commit verification claim.
  </acceptance_criteria>
  <done>Frozen implementation I3 has a separate adversarial verdict and an evidence-only descendant R3.</done>
  <commit>docs(phase-00): adversarially review generation 3 implementation</commit>
</task>

<task type="auto" category="verify" id="00-04-08">
  <name>Task 8: Independently verify R3 and create evidence-only candidate V3</name>
  <files>.planning/phases/00-planning-truth-and-containment/00-VERIFICATION.md</files>
  <read_first>.planning/phases/00-planning-truth-and-containment/00-04-PLAN.md, .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md, .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md, .planning/phases/00-planning-truth-and-containment/00-SECURITY-REVIEW.md, .planning/TEST-CONTRACT.md, .planning/BLOCKERS.md, all P3..R3 changed paths</read_first>
  <action>
    Execution owner: an independent verifier who is neither builder nor Task 7 reviewer. At exact clean R3, rerun the full TEST-CONTRACT and every acceptance criterion, allowlist/immutable comparison and external-evidence hash check. Write external `VERIFICATION-RECEIPT.md` bound to R3 commit/tree. Only this verifier may update the internal verification report; it must retain Phase 0 in_progress/NO-GO and state its verdict binds R3. Commit only that report. Resolve the resulting commit as V3 and prove parent(V3)=R3 and `R3..V3` contains only the report. No source repair is allowed in this task.
  </action>
  <verify>Full exact-R3 matrix; typed acceptance table; receipt hash; V3 parent/path proof; clean status and zero remotes.</verify>
  <acceptance_criteria>
    - Verifier is distinct from builders and reviewer and binds its receipt to exact R3.
    - Every mandatory test and plan criterion passes without missing/stale evidence.
    - V3 differs from R3 only by VERIFICATION and contains no source repair or phase/requirement completion.
  </acceptance_criteria>
  <done>V3 is an evidence-only descendant of the independently verified R3 tree, ready for an exact-final-commit admission rerun.</done>
  <commit>docs(phase-00): independently verify generation 3 implementation</commit>
</task>

<task type="auto" category="verify" id="00-04-09">
  <name>Task 9: Admit exact V3 as local candidate C3</name>
  <files>none; external CANDIDATE-MANIFEST-v3.md only</files>
  <read_first>.planning/phases/00-planning-truth-and-containment/00-04-PLAN.md, .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md, .planning/phases/00-planning-truth-and-containment/00-SECURITY-REVIEW.md, .planning/phases/00-planning-truth-and-containment/00-VERIFICATION.md, .planning/TEST-CONTRACT.md, all external Generation 3 receipts</read_first>
  <action>
    Execution owner: a final-admission agent distinct from builders, reviewer and verifier. Freeze clean V3 and rerun the complete TEST-CONTRACT at exact V3 so the final metadata commit itself is covered. Prove P3..V3 allowlist containment, immutable equality, reviewer/verifier ancestry, and that source/test tree content equals the reviewed I3 source/test content except the two authorized evidence reports. Run the unchanged repository-artifact validator and redacted Gitleaks 8.30.1 git/tree wrappers, strict fsck, one-branch/zero-tag/zero-remote checks, ref/reflog/alternate/common-dir/shared/shallow/partial/promisor/replace/graft checks and physical-versus-reachable equality. Re-snapshot C2 and compare its HEAD/tree/refs/config/status/object counts and v2-manifest hash to Task 1.

    Only after all checks pass, define `C3 = V3` in external `CANDIDATE-MANIFEST-v3.md`. Record C2/D3/P3/I3/R3/V3/C3 commits/trees, plan and receipt hashes, bytecode manifest/tool hashes, safe scanner counts, tool versions and typed verdicts. Do not edit or commit any repository file. A failure returns to a typed remediation cycle and V3 is not admitted.
  </action>
  <verify>Exact-V3 full matrix; silent strict fsck; zero safe scanner findings; repository topology/object checks; C2 before/after equality; final clean worktree and zero remotes.</verify>
  <acceptance_criteria>
    - Exact V3, not merely I3 or R3, passes the complete local matrix and repository admission checks.
    - Gitleaks history/tree findings are zero through unchanged redacted wrappers; raw matches are never persisted.
    - Exactly one expected branch and zero remotes/tags/replace/grafts/alternates/shared/partial/shallow state exist; physical equals reachable objects.
    - C2 remains exact, clean, isolated and unchanged from both the v2 manifest and Task 1 snapshot.
    - The external manifest states local candidate only, Phase 0 in_progress/NO-GO, and no push/deploy/RPC/wallet/key/provider/money action.
  </acceptance_criteria>
  <done>Exact clean V3 is admitted as local-only C3 without changing Git or crossing a human/professional/production gate.</done>
</task>

</tasks>

<verification>
Before declaring this plan locally complete:

- [ ] P3 is plan-only over D3; C2 is unchanged.
- [ ] Each task has one atomic commit except external-only final admission.
- [ ] Compiler config-only bytecode proof is exact and scope-limited.
- [ ] Web, identity, issuance and factory adversarial tests pass.
- [ ] I3, R3 and V3 parent/path contracts pass.
- [ ] Exact V3 passes the full local matrix and repository admission.
- [ ] CANDIDATE-MANIFEST-v3.md binds exact C3=V3.
- [ ] Phase 0 and requirements remain incomplete; NO-GO remains.
- [ ] No remote, push, deployment, RPC, wallet, key, provider, real-money or irreversible action occurred.
</verification>

<success_criteria>

- All nine tasks complete in order with role separation and exact receipts.
- Every mandatory check passes with nonzero test discovery and no stale evidence.
- All changed repository paths are allowlisted and all immutable paths match C2.
- Exact C3 is clean, isolated and locally admitted without production-readiness claims.
</success_criteria>

<output>
Repository outputs are the allowlisted implementation/evidence files. External outputs are stored only under `C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721`.
</output>

## Failure and rollback

- Any out-of-allowlist change, immutable-file drift, scanner finding, bytecode mismatch, mandatory-test failure, stale evidence, non-independent verdict or ambiguous approval is a hard stop.
- One remediation commit may address a typed root cause; a second failure for the same root cause exhausts the two-cycle ceiling and requires a newly accepted plan.
- Never amend C2, D3, P3, I3, R3 or an admitted C3. Preserve failed attempts and safe receipts.
- Remove a disposable directory only after resolving and checking its absolute path is beneath the exact generated temporary root. The clean commits and external manifests remain the recovery source.
