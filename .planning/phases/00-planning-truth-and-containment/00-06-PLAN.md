---
phase: 00-planning-truth-and-containment
plan: 06
type: execute
name: generation 3 GO-2026-5970 dependency remediation
status: proposed
gap_closure: true
wave: 1
depends_on: []
autonomous: true
risk_class: critical
requirements: [BASE-03, BASE-04, BASE-06, BASE-07]
user_setup: []
execution_mode: controller-dispatched-serial-tasks
requirements_completion: forbidden
files_modified:
  - .planning/phases/00-planning-truth-and-containment/00-06-PLAN.md
  - go.mod
  - go.sum
  - .planning/scripts/validate-planning.ps1
  - .planning/EVIDENCE-REGISTER.md
  - .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md
  - .planning/BLOCKERS.md
  - .planning/STATE.md
  - .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md
must_haves:
  truths:
    - "The newly reachable GO-2026-5970 finding is remediated before any Generation 3 evidence freeze."
    - "The dependency change is limited to canonical x/text and x/sync version/checksum changes; application source is unchanged."
    - "The current-phase plan set is exactly 00-01 through 00-06, while authoritative Phase 0 progress remains zero and NO-GO."
    - "The complete Generation 3 matrix passes at exact D5 before I3 and again at exact I3."
    - "Independent I3/R3/V3 review, verification and local admission ancestry remains intact."
  artifacts:
    - "go.mod"
    - "go.sum"
    - ".planning/scripts/validate-planning.ps1"
    - ".planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md"
  key_links:
    - "P5 is a plan-only child of P4; D5 is a two-file dependency-only child of P5."
    - "I3 is a six-file evidence child of D5; R3 and V3 retain their one-file report ancestry."
    - "Plans 00-04, 00-05 and 00-06 form the Generation 3 union allowlist."
---

# Plan 00-06: Generation 3 GO-2026-5970 dependency remediation

## Objective

Remediate the newly disclosed, reachable `GO-2026-5970` finding without
changing application code, weakening the test contract or losing the
independent Generation 3 assurance ancestry.

## Approved local authority

The user approved continued execution of the controlled local delivery loop on
2026-07-23. That approval authorizes local planning, implementation, exact
dependency resolution, full local testing, independent review, independent
verification and local commits.

It does not authorize a remote, push, deployment, RPC, provider, wallet, key,
funds, real assets, production network, production configuration, professional
approval or irreversible action.

## Trigger and confirmed root cause

At exact clean P4
`f081dfc211a767e660d93c0bd33f35b2f30a9803`, Plan 00-05 Task 2 passed:

- planning validation: 12 phases, zero authoritative progress and 89 unchecked
  requirements;
- repository-artifact validation: 479 tracked paths and 13 reachable commits;
- CI policy, production Compose policy and actionlint;
- Go tests: 12 tested packages plus 13 packages without tests;
- Go vet, golangci-lint v2.12.2 and gosec over 85 files and 22,573 lines.

Pinned govulncheck v1.6.0 then exited 3 because its database, updated after the
earlier successful Generation 3 run, now reports reviewed `GO-2026-5970`
(`CVE-2026-56852`). Invalid UTF-8 can make `golang.org/x/text/unicode/norm`
loop indefinitely.

The finding is reachable through:

`internal/db/db.go:23 db.Connect -> pgxpool.NewWithConfig ->
x/text/secure/precis -> x/text/unicode/norm`.

This is vulnerability-database drift, not an application-source regression.
The first fixed `golang.org/x/text` release is v0.39.0. Its Go 1.25 module
requirement is compatible with the repository's Go 1.26.0 directive and pinned
Go 1.26.5 toolchain.

An isolated alternate-modfile simulation followed by `go mod tidy` proved the
canonical repository delta:

- `golang.org/x/text` v0.33.0 -> v0.39.0;
- `golang.org/x/sync` v0.19.0 -> v0.21.0;
- replace the matching two module and two `go.mod` checksum entries; and
- no application, test, workflow or configuration source change.

The simulation is retained outside Git at:

`C:\Users\danie\AppData\Local\Temp\blockxone-go5970-sim-3d191e7e86e44ee38db778d2a4873406`.

## Narrow supersession

This plan supersedes only the following Plan 00-05 assumptions:

- the exact current-phase plan set becomes `00-01-PLAN.md` through
  `00-06-PLAN.md`;
- `STATE.progress.total_plans` becomes 6;
- one recognized `00-04-SUMMARY.md` produces GSD artifact progress 17%, not
  20%, because the pinned tool applies `Math.round(1 / 6 * 100)`;
- the accepted ancestry becomes `P4 -> P5 -> D5 -> I3 -> R3 -> V3`; and
- the six-file I3 path comparison is `D5..I3`, while `P5..D5` is exactly
  `go.mod` and `go.sum`.

All other Plan 00-04 and Plan 00-05 controls, retained blockers, non-claims,
role-separation requirements, immutable boundaries and final NO-GO status
remain binding.

ROADMAP, REQUIREMENTS, global GSD tooling, Plans 00-04 and 00-05 are immutable.
No `00-05-SUMMARY.md` or `00-06-SUMMARY.md` may be created.

<tasks>

<task type="auto" category="ops" id="00-06-01">
  <name>Task 1: Freeze and independently accept plan-only P5</name>
  <files>none; external GEN3-P5-GO5970-PLAN-ACCEPTANCE.md only</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-06-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-05-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-PLAN.md,
    .planning/TEST-CONTRACT.md,
    .planning/scripts/validate-planning.ps1,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    go.mod,
    go.sum
  </read_first>
  <action>
    Execution owners: controller and independent plan checker.

    Define P4 as
    `f081dfc211a767e660d93c0bd33f35b2f30a9803` with tree
    `f10a72b7890c0ef5e6051214002e5f38bb1a375c`. Commit this plan and no other
    path as P5 with message
    `docs(phase-00): accept GO-2026-5970 remediation`.

    Prove `parent(P5)=P4` and `git diff --name-only P4..P5` equals exactly
    `.planning/phases/00-planning-truth-and-containment/00-06-PLAN.md`.
    Prove one local branch, zero remotes/tags/replace refs/grafts/alternates,
    strict fsck, clean status and no generated repository path.

    Write external `GEN3-P5-GO5970-PLAN-ACCEPTANCE.md` under the established
    Generation 3 evidence directory. Bind P4/P5 commits and trees, plan
    SHA-256, the 2026-07-23 approval, official vulnerability identity,
    independent checker verdict, nine-path Plan 00-06 allowlist and union
    semantics with Plans 00-04 and 00-05.
  </action>
  <verify>
    Independent plan checker returns VERIFICATION PASSED; P5 is a clean
    plan-only descendant; receipt hash is recorded; no production action
    occurs.
  </verify>
  <acceptance_criteria>
    - `parent(P5)` is exactly P4.
    - `P4..P5` changes only `00-06-PLAN.md`.
    - Plan 00-06 names exactly nine repository paths.
    - ROADMAP, REQUIREMENTS, Plans 00-04/00-05 and global GSD files are absent
      from the allowlist.
    - Receipt states local-only, Phase 0 in_progress, zero production progress,
      six total plans and NO-GO.
    - No `00-05-SUMMARY.md` or `00-06-SUMMARY.md` exists.
  </acceptance_criteria>
  <done>P5 is an independently checked plan-only descendant with exact scope.</done>
  <commit>docs(phase-00): accept GO-2026-5970 remediation</commit>
</task>

<task type="auto" category="code" id="00-06-02">
  <name>Task 2: Commit the canonical two-file dependency remediation as D5</name>
  <files>go.mod, go.sum</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-06-PLAN.md,
    .planning/TEST-CONTRACT.md,
    go.mod,
    go.sum,
    internal/db/db.go
  </read_first>
  <action>
    Execution owner: dependency remediator who did not check this plan.

    At exact clean P5, use only pinned Go 1.26.5 with `GOTOOLCHAIN=local`.
    Run the canonical exact-version update for
    `golang.org/x/text@v0.39.0`, then `go mod tidy`.

    Require the resulting `go.mod` delta to contain only:

    - x/sync v0.19.0 -> v0.21.0; and
    - x/text v0.33.0 -> v0.39.0.

    Require the `go.sum` delta to replace only those versions' module and
    `go.mod` checksum entries, matching the retained isolated simulation.
    Reject any additional direct, indirect, toolchain, Go directive, source or
    generated-file change.

    Run `go mod tidy` a second time and prove no diff, then `go mod verify`,
    module-graph selection checks, all Go tests with nonzero test inventory,
    Go vet, golangci-lint v2.12.2, gosec v2.25.0 and govulncheck v1.6.0.
    Require zero reachable vulnerabilities. Imported or required-module
    informational findings must be recorded but may not be represented as
    reachable findings.

    Commit exactly `go.mod` and `go.sum` as D5 with message
    `fix(deps): remediate GO-2026-5970`.
  </action>
  <verify>
    `parent(D5)=P5`; `P5..D5` is exactly go.mod/go.sum; the canonical delta
    matches the simulation; complete Go gates pass at exact D5; clean status,
    zero remotes and no generated path remain.
  </verify>
  <acceptance_criteria>
    - No application or test source changes.
    - Exact selected x/text is v0.39.0 and x/sync is v0.21.0.
    - Second tidy is stable and `go mod verify` passes.
    - Go tests, vet, lint and gosec pass.
    - Govulncheck reports zero reachable vulnerabilities.
    - D5 changes exactly two repository paths.
  </acceptance_criteria>
  <done>D5 is a clean two-file dependency-only remediation with zero reachable Go vulnerabilities.</done>
  <commit>fix(deps): remediate GO-2026-5970</commit>
</task>

<task type="auto" category="docs" id="00-06-03">
  <name>Task 3: Re-run the complete matrix, freeze six-file I3 and prove exact I3</name>
  <files>
    .planning/scripts/validate-planning.ps1,
    .planning/EVIDENCE-REGISTER.md,
    .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md,
    .planning/BLOCKERS.md,
    .planning/STATE.md,
    .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-06-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-05-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-PLAN.md,
    .planning/TEST-CONTRACT.md,
    .planning/scripts/validate-planning.ps1,
    .planning/EVIDENCE-REGISTER.md,
    .planning/phases/00-planning-truth-and-containment/00-EVIDENCE.md,
    .planning/BLOCKERS.md,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Execution owner: validator/evidence builder who authored neither this plan
    nor its independent verdict.

    At exact clean D5, run every command in TEST-CONTRACT with pinned
    toolchains. Record planning/repository/CI/Compose/actionlint; Go tests,
    vet, golangci-lint, gosec and govulncheck; web preflight/config/runner,
    root/package tests, lint, audits, build and containment; contract preflight,
    compile, typecheck, tests and audits; raw Compose; and exact bytecode
    evidence validation. Missing, skipped or zero-test suites are fatal.

    Apply the Plan 00-05 path-specific validator design with only these
    Plan 00-06 supersessions:

    1. Require the exact plan set `00-01-PLAN.md` through `00-06-PLAN.md`.
    2. Continue recognizing only `00-04-SUMMARY.md`; reject `SUMMARY.md`,
       every other `*-SUMMARY.md`, case variant and additional summary.
    3. Require pinned `init milestone-op.completed_phases` to equal
       `summaryPresent` 0 or 1 and label it only as GSD's
       summary-bearing-phase counter.
    4. Require Phase 0 from `roadmap analyze` to have `plan_count=6`,
       `summary_count=summaryPresent`, `completed_phases=0`,
       `current_phase='0'`, and:
       - without summary: `disk_status='planned'`, artifact progress 0;
       - with summary: `disk_status='partial'`, artifact progress 17.
    5. If the summary exists, require exact Plan 00-05 summary metadata and
       statements: plan 04, implementation frozen for independent review,
       NO-GO, no completed requirements, Phase 0 in_progress, builder evidence
       only and Tasks 7-9 still mandatory.
    6. Independently require STATE in_progress, completed phases/plans zero and
       total plans 6.
    7. Independently require unchecked/in-progress Phase 0 ROADMAP status.
    8. Require exactly 89 unchecked, unique requirement IDs and zero checked
       requirement IDs.

    Mutation-test wrong/missing/extra/case-variant plan or summary filenames,
    wrong summary metadata, completed STATE, checked/completed ROADMAP and
    checked/duplicate/missing requirements. The validator must accept exact
    no-summary D5 and exact-one-summary I3 states.

    Recreate only the five builder-evidence files. Record the P4 scanner
    failure, database-drift diagnosis, P5/D5 ancestry, exact module delta,
    complete D5 and I3 results, warnings, hashes and all retained NO-GO
    blockers. Set STATE total_plans to 6 and keep completed values zero. Do not
    edit ROADMAP or REQUIREMENTS.

    Commit exactly the validator and five builder-evidence paths as I3 with
    message `docs(phase-00): record generation 3 local matrix`.

    At exact clean I3, rerun the complete TEST-CONTRACT and bytecode validation.
    Write external `TASK6-I3-BUILDER-RECEIPT.md` bound to P4/P5/D5/I3
    commits/trees, commands, exits, counts, warnings, module selections, hashes
    and generated-path absence. Recoverably quarantine generated outputs
    beneath a newly validated OS-temp directory; never recursively delete or
    overwrite.
  </action>
  <verify>
    Full matrix passes at exact D5 and exact I3; validator passes D5 and I3
    semantics plus mutation tests; `D5..I3` is exactly six allowed paths;
    immutable files match; status clean and remotes zero.
  </verify>
  <acceptance_criteria>
    - D5 and I3 mandatory commands all exit zero with nonzero tests.
    - `D5..I3` changes exactly the validator and five Task 6 evidence paths.
    - Validator accepts only exact 0-summary/0% D5 and
      1-summary/17%-partial I3 artifact states.
    - Authoritative STATE remains in_progress with total_plans 6 and every
      completed value zero.
    - ROADMAP and REQUIREMENTS are byte-identical to P4.
    - Summary is builder-only, requirements-completed empty and NO-GO.
    - Exact I3 is clean, remote-free and has no generated path.
  </acceptance_criteria>
  <done>I3 is a clean six-file evidence freeze over a remediated D5 tree.</done>
  <commit>docs(phase-00): record generation 3 local matrix</commit>
</task>

<task type="auto" category="verify" id="00-06-04">
  <name>Task 4: Resume independent R3, V3 and local C3 admission</name>
  <files>none beyond Plan 00-04 Task 7 and Task 8 one-file reports</files>
  <read_first>
    .planning/phases/00-planning-truth-and-containment/00-06-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-05-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-PLAN.md,
    .planning/phases/00-planning-truth-and-containment/00-04-SUMMARY.md,
    .planning/TEST-CONTRACT.md,
    .planning/BLOCKERS.md
  </read_first>
  <action>
    Execution owners remain three distinct agents who authored neither the
    dependency/evidence tree nor each other's report.

    Resume Plan 00-04 Tasks 7-9 with Plans 00-05 and 00-06 binding:

    - Task 7 reviews exact I3 and commits only `00-SECURITY-REVIEW.md` as R3.
      Require `parent(R3)=I3`.
    - Task 8 reruns the complete matrix at exact R3 and commits only
      `00-VERIFICATION.md` as V3. Require `parent(V3)=R3`.
    - Task 9 reruns the complete matrix and final repository admission at exact
      V3, writes only external `CANDIDATE-MANIFEST-v3.md`, and defines C3=V3.

    All allowlist comparisons use the union of Plans 00-04, 00-05 and 00-06.
    Source/test content at R3 and V3 must equal reviewed I3 source/test content;
    only the authorized evidence reports may differ. Re-snapshot immutable C2
    and retain Phase 0 in_progress, zero completed requirements/plans/phases,
    total plans 6 and release NO-GO.
  </action>
  <verify>
    Independent identities; exact I3/R3/V3 binding; one-file report diffs;
    exact-V3 complete matrix; union allowlist and immutable equality; strict
    topology/object/scanner checks; C2 equality; zero remotes.
  </verify>
  <acceptance_criteria>
    - Task 7 has no unresolved critical/high, dependency or false-success
      finding.
    - `I3..R3` is only SECURITY-REVIEW and `R3..V3` only VERIFICATION.
    - Exact V3 passes every TEST-CONTRACT and repository admission command.
    - External C3 manifest binds P3, D4, P4, P5, D5, I3, R3, V3 and C3=V3.
    - Manifest states local candidate only, Phase 0 in_progress/NO-GO and no
      production action.
  </acceptance_criteria>
  <done>Generation 3 is independently reviewed, verified and locally admitted under the checked union boundary.</done>
</task>

</tasks>

<verification>

- [ ] P5 is a plan-only child of exact P4.
- [ ] D5 is a two-file dependency-only child of P5.
- [ ] GO-2026-5970 is absent from reachable govulncheck findings at D5, I3,
      R3 and V3.
- [ ] Current phase contains exactly six plans and only one permitted summary.
- [ ] GSD 0/1 and 0/17 artifact counters are never called production progress.
- [ ] STATE remains in_progress with six total and zero completed plans.
- [ ] ROADMAP Phase 0 and all 89 requirements remain unchecked.
- [ ] Complete matrix passes at D5, I3, R3 and exact V3.
- [ ] I3/R3/V3 parent and exact-path contracts pass.
- [ ] No `00-05-SUMMARY.md`, `00-06-SUMMARY.md` or unapproved summary exists.
- [ ] No remote, push, deployment, RPC, provider, wallet, key, funds, real
      assets, production configuration or irreversible action occurs.

</verification>

<success_criteria>

- P5 has an independent plan-checker verdict and external acceptance receipt.
- D5 contains only the canonical x/text/x/sync module remediation.
- Exact I3 contains complete builder evidence without claiming independent
  review or production completion.
- Exact R3 and V3 retain independent one-file evidence ancestry.
- Exact C3=V3 is admitted locally while Phase 0 remains in_progress and NO-GO.

</success_criteria>

<failure_and_rollback>

- Any extra module/path/version, product-code change, extra summary, completed
  checkbox/status, reachable vulnerability, matrix failure, stale evidence or
  non-independent verdict is a hard stop.
- One dependency-remediation cycle is permitted. A repeat of the same root
  cause requires another checked plan.
- Never amend P4, P5, D5, I3, R3, V3 or admitted C3.
- Preserve failed attempts and receipts; move only exact validated generated
  paths to recoverable quarantine.

</failure_and_rollback>

<output>

Repository outputs are limited to this plan, the exact two-file dependency
remediation, the six-file I3 freeze and the original Plan 00-04 one-file
reviewer/verifier reports. External receipts remain under:

`C:\Users\danie\Documents\BlockXOne Phase0 Recovery 20260719\clean-root-20260720\generation-3-20260721`.

</output>
