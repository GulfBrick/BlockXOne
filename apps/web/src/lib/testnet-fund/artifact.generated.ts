// Cloud compilation replaces this null fallback with verified solc output.
// Never insert hand-written or inferred bytecode here.
export const demoFundArtifact: {
  abi: readonly string[]
  bytecode: string
  deployedBytecode: string
  compilerVersion: string
  sourceSha256: string
} | null = null
